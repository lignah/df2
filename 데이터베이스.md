# 데이터베이스

---

# 1강

---

## 1. (기출) — 마스터 데이터 관리

**문제**
기업의 마스터 데이터와 이를 관리하는 방법에 대한 설명으로 옳지 않은 것은?

**보기**
- 가. 마스터 데이터는 비즈니스 트랜잭션에 동일한 형태로 사용되는 정보로써 여러 개의 코드 또는 Key와 업무적 기준을 가지면서 장기간 동일하게 유지되어야 하는 데이터를 말한다.
- 나. 마스터 데이터를 생성하기 위한 소스 데이터가 우선적으로 파악되어야 한다.
- 다. 소스 시스템에서 대량 데이터를 추출한 데이터는 별도의 변형과정 없이 각 소스의 포맷대로 하나의 마스터 데이터로 통합되어야 한다.
- 라. 마스터 데이터 관리를 위해서는 마스터 데이터 인프라스트럭처의 구축이 필요한데, 데이터베이스 관리 시스템, 메시징 및 커뮤니케이션, 인프라스트럭처와 툴 그리고 확장성 있는 하드웨어 플랫폼이 기반이 되어야 한다.

**정답:** 다

**해설**
소스에서 추출한 데이터는 통합 전 **변형·정제·표준화**가 필요하다. 포맷 그대로 통합하면 일관성 문제 → 다가 옳지 않음.

---

## 2. (기출) — 분산 설계

**문제**
분산 설계에 대한 설명으로 옳은 것은?

**보기**
- 가. 분산 데이터베이스의 설계 목적은 중복 데이터의 최소화이다.
- 나. 분산 데이터베이스는 논리적으로 통합된 데이터를 물리적인 데이터베이스로 구현할 때 적용하는 것이다.
- 다. 분산 데이터베이스는 논리적으로는 하나의 통합된 데이터베이스에서 관리되어야 한다.
- 라. 분산 데이터베이스는 동일 전산실 환경이 아닌 여러 지역에 분산된 데이터베이스에 구축되는 것을 말한다.

**정답:** 나

**해설**
분산 DB는 논리적으로 통합·물리적으로 분산 구현. 가(중복 최소화는 목적이 아님), 다(반드시 단일 통합 관리 X), 라(동일 전산실에서도 분산 가능)는 옳지 않음.

---

## 3. (기출 14회·15회·17회·18회) — SQLite

**문제**
오픈소스 데이터베이스로 가볍고 빠른 특성 때문에 간단한 정보를 체계적으로 저장하는 응용 프로그램이나 스마트폰, 임베디드 장비에 탑재되는 소프트웨어에서 주로 사용하는 DBMS(Database Management System)로 구글 안드로이드 운영체제에 기본 탑재된 데이터베이스는 무엇인가?

**보기**
- 가. SQLite
- 나. Microsoft SQL Server
- 다. Oracle
- 라. MySQL

**정답:** 가

**해설**
가볍고 파일 기반·무설치, 안드로이드 기본 탑재 → **SQLite**.

---

## 4. (기출 15회) — 데이터 중복성 문제점

**문제**
데이터 중복성으로 인해 야기될 수 있는 문제점에 관한 설명 중 옳지 않은 것은?

**보기**
- 가. 데이터 간에 불일치가 일어나 내부적으로 데이터 일관성(consistency)이 없게 된다.
- 나. 같은 데이터가 여러 곳에 중복 저장되어 있는 경우에 모두 같은 수준의 보안을 유지하는 것이 쉽다.
- 다. 갱신 비용이 높게 되어 경제성이 떨어지게 된다.
- 라. 데이터의 무결성(integrity)을 유지하기가 어렵다.

**정답:** 나

**해설**
중복 저장 시 동일 수준 보안 유지는 **어렵다**. 나가 옳지 않은 설명.

---

# 2강

---

## 1. (기출 15회) — 개념 스키마

**문제**
다음 중 개념 스키마에 대한 설명으로 옳은 것은?

**보기**
- 가. 데이터베이스의 사용자나 응용 프로그래머가 접근하는 데이터베이스를 정의한 것이다.
- 나. 모든 응용 시스템들이나 사용자들이 필요로 하는 데이터를 통합한 조직 전체의 데이터베이스를 기술한 것이다.
- 다. 저장 장치의 입장에서 데이터베이스 전체가 저장되는 방법을 명세한 것이다.
- 라. 공용의 의미보다는 어느 개인이나 특정 응용에 한정된 논리적 데이터 구조이다.

**정답:** 나

**해설**
개념 스키마는 조직 전체 통합 DB의 논리적 구조. 가·라는 외부 스키마, 다는 내부 스키마.

---

## 2. (기출 15회·19회) — Firefox SQLite 파일

**문제**
다음 중 firefox에 사용되는 SQLite 파일이 아닌 것은?

**보기**
- 가. cookie.sqlite
- 나. formhistory.sqlite
- 다. places.sqlite
- 라. storages.sqlite

**정답:** 라

**해설**
`storages.sqlite`는 Firefox에서 쓰이지 않는 이름. (관련 파일은 webappstore.sqlite·storage.sqlite 등)

---

## 3. (기출 16회) — 데이터웨어하우스 특징

**문제**
데이터웨어하우스의 특징에 대한 설명으로 적절하지 않은 것은?

**보기**
- 가. 통합성 : 혼재한 DB로부터의 데이터 통합하여 데이터의 표현을 단일화
- 나. 시계열성 : 시간에 따른 변경 정보를 반영하여 시점별로 분석이 가능
- 다. 비휘발성 : 데이터 변경 없이 리포팅을 위한 read only를 사용하여, 한 시점에서 대량의 데이터가 적재되고 액세스되는 반면, 데이터의 갱신이 발생하지 않는 조회 전용 데이터
- 라. 업무중심성 : 주제중심이 아닌 업무중심으로 효율성을 기함

**정답:** 라

**해설**
DW는 **주제중심**이지 업무중심이 아님 → 라가 부적절.

---

## 4. (기출 19회) — DBMS 장점

**문제**
파일 시스템(file system)과 비교하여 DBMS가 가지는 장점으로 옳지 않은 것은 무엇인가?

**보기**
- 가. 모든 데이터를 데이터베이스로 통합하여 관리하므로 중복성과 불일치가 감소된다.
- 나. 프로그램과 데이터를 분리함으로써 데이터의 변경으로 인해 프로그램을 수정해야 하는 종속성(dependency)이 감소된다.
- 다. 동시성 제어(concurrency control) 기법을 제공하므로 파일 시스템에 비해 빠른 응답 시간을 제공한다.
- 라. 데이터 접근 시 시스템이 고장 나는 경우에도 고장 나기 이전의 일관된 데이터베이스 상태로 복구할 수 있다.

**정답:** 다

**해설**
동시성 제어는 일관성 유지용이며 **빠른 응답**을 보장하지 않고 오히려 오버헤드가 될 수 있음.

---

## 5. (기출 21회) — 데이터 모델 개념

**문제**
다음 중 데이터 모델의 개념으로 가장 적절한 것은?

**보기**
- 가. 컴퓨터 세계의 데이터 구조를 현실 세계의 데이터 구조로 기술하는 도구다.
- 나. 현실 세계의 데이터 구조를 컴퓨터 세계의 데이터 구조로 기술하는 도구이다.
- 다. 가상 세계의 데이터 구조를 현실 세계의 데이터 구조로 기술하는 도구이다.
- 라. 가상 세계의 특정한 한 부분을 표현한 것이다.

**정답:** 나

**해설**
데이터 모델은 **현실 세계 → 컴퓨터 세계**로 구조를 기술하는 도구.

---

# 3강

---

## 1. (기출 14회) — 관계(Relationship)

**문제**
관계(Relationship)에 대한 설명으로 옳지 않은 것은?

**보기**
- 가. 엔터티(Entity)와 엔터티 사이의 관계도 집합이다.
- 나. 집합 간에 존재하는 무수히 많은 관계 중에 직접 종속인 것만을 관계로 보고 모델링한다.
- 다. 두 엔터티 간에 하나 이상의 관계를 정의하는 것은 바람직하지 않다.
- 라. 다대다(M:M) 관계는 복잡성을 증가시키므로, 발견 즉시 두 개의 일대다(1:M) 관계를 갖는 관계 엔터티로 분해된다.

**정답:** 다

**해설**
두 엔터티 간 **복수 관계** 정의는 가능·바람직할 수 있음. 다가 옳지 않음.

---

## 2. (기출 16회) — 엔티티 용어

**문제**
데이터베이스에 관한 아래 설명에 해당하는 용어로 적합한 것은?

> 이것은 실세계에서 독립적으로 존재한다. 이것은 물리적(개인, 차, 집, 종업원 등) 혹은 개념적(회사, 직업, 대학 과정 등)으로 존재한다.

**보기**
- 가. view
- 나. entity
- 다. value
- 라. relationship

**정답:** 나

**해설**
실세계 독립 존재(물리·개념) → **entity**.

---

## 3. (기출 19회) — ER 모델 구성 요소

**문제**
ER 모델에 대한 다음의 설명 중 괄호에 들어갈 내용을 순서대로 나열한 것은 무엇인가?

> (㉠)은 데이터 수집의 대상이 되는 실 정보세계에 존재하는 사물들로, 개념적인 것과 물리적인 것으로 나눌 수 있다. (㉡)은 (㉠)의 구조를 나타내는 특성을 의미한다. (㉢)은 (㉠)사이에 존재하는 연관성을 의미한다.

**보기**
- 가. ㉠-엔티티, ㉡-속성, ㉢-추상화
- 나. ㉠-엔티티, ㉡-속성, ㉢-관계
- 다. ㉠-속성, ㉡-엔티티, ㉢-추상화
- 라. ㉠-속성, ㉡-엔티티, ㉢-관계

**정답:** 나

**해설**
㉠ 엔티티 · ㉡ 속성 · ㉢ 관계.

---

## 4. (기출 20회) — 널(null) 값

**문제**
다음 중 널(null) 값에 대한 설명으로 옳지 않은 것은?

**보기**
- 가. 아직 모르는 값이다.
- 나. 적합한 값이 없다.
- 다. 숫자 0이나 공백 문자와 같은 의미로 해석해야 한다.
- 라. 기본키로 선택된 속성은 널 값을 가질 수 없다.

**정답:** 다

**해설**
널은 0·공백과 **다른** 개념. 다를 같은 의미로 해석하면 틀림.

---

# 4강

---

## 1. (기출 15회) — 디비전 연산 결과

**문제**
다음의 두 릴레이션 SC와 C1에 대한 디비전 연산 SC ÷ C1 연산을 수행한 결과는?

**릴레이션 SC**

| 학번 | 교과목번호 |
|------|------------|
| 100 | C413 |
| 100 | E412 |
| 200 | C120 |
| 300 | C312 |
| 300 | C324 |
| 300 | C413 |
| 400 | C312 |
| 400 | C324 |
| 400 | C413 |
| 400 | E412 |

**릴레이션 C1**

| 교과목번호 |
|------------|
| C312 |
| C413 |

**보기**
- 가. 학번 100, 200, 300, 400
- 나. 학번 200, 300, 400
- 다. 학번 300, 400
- 라. 학번 400

**정답:** 다

**해설**
디비전은 C1의 **모든** 교과목번호(C312, C413)를 수강한 학번만 추출.
학번 100은 C312 미수강 → 제외, 학번 200은 C312·C413 모두 미수강 → 제외.
학번 300·400은 C312와 C413을 모두 수강 → 포함. 결과: **300, 400**.

---

## 2. (기출 18회) — 셀렉션(Selection)

**문제**
한 릴레이션에서 주어진 조건식을 만족하는 튜플만을 선택하는 관계형 대수(Relational Algebra)는 무엇인가?

**보기**
- 가. 카티션 프로덕트(Cartesian Product)
- 나. 프로젝션(Projection)
- 다. 조인(Join)
- 라. 셀렉션(Selection)

**정답:** 라

**해설**
조건식을 만족하는 **튜플(행)** 선택 = **셀렉션**.
가(모든 튜플 조합 생성), 나(특정 **속성·열** 선택), 다(릴레이션 **결합**)은 목적이 다름.

---

## 3. (기출 18회) — 조인(Join) 정의

**문제**
다음이 무엇에 대한 설명인지 보기에서 고르시오.

> 공통 속성을 중심으로 두 개의 릴레이션을 하나로 합쳐서 새로운 릴레이션을 만드는 연산

**보기**
- 가. Select
- 나. Join
- 다. Project
- 라. Division

**정답:** 나

**해설**
공통 속성 기준 결합 → **Join**.
가(조건 튜플 선택), 다(특정 속성·열 선택), 라(한 릴레이션을 다른 릴레이션으로 나누는 연산)는 해당 설명이 아님.

---

## 4. (기출 20회) — SQL UNION

**문제**
한 번에 두 개의 테이블에서 데이터를 조회할 때 사용하며, 특히 SQL 인젝션 공격에서 다른 테이블의 내용을 조회할 때 사용하는 명령어는 무엇인가?

**보기**
- 가. SUM
- 나. INSERT
- 다. PLUS
- 라. UNION

**정답:** 라

**해설**
두 SELECT 결과를 결합·타 테이블 조회에 악용되는 것은 **UNION**.
가(집계 합계), 나(행 추가), 다(SQL에 일반적 PLUS 연산자 없음)는 해당하지 않음.

---

# 5강

---

## 1. (기출 14회) — DROP TABLE CASCADE

**문제**
다음 SQL 문의 실행결과를 가장 잘 설명한 것은?

```sql
DROP TABLE 급여 CASCADE
```

**보기**
- 가. 급여 테이블을 제거한다.
- 나. 급여 테이블이 참조 중이면 제거하지 않는다.
- 다. 급여 테이블을 제거할지의 여부를 사용자에게 다시 질의한다.
- 라. 급여 테이블을 참조하는 테이블과 급여 테이블을 제거한다.

**정답:** 라

**해설**
CASCADE는 급여 테이블뿐 아니라 **참조하는 외래키·관련 테이블까지** 함께 삭제.
가(CASCADE 설명 부족), 나(RESTRICT에 해당), 다(추가 확인 요구 없음).

---

## 2. (기출 17회) — DDL 테이블 삭제 명령어

**문제**
데이터베이스 정의어(Data Definition Language)에서 테이블을 삭제하는 명령어는 무엇인가?

**보기**
- 가. Select Table
- 나. Alter Table
- 다. Drop Table
- 라. Create Table

**정답:** 다

**해설**
테이블 삭제 DDL = **DROP TABLE**.
가(조회 DML), 나(구조 변경), 라(생성)는 삭제가 아님.

---

## 3. (기출 20회) — DBMS 언어 설명

**문제**
DBMS(Data Base Management System) 언어에 관한 설명으로 옳지 않은 것은?

**보기**
- 가. DDL: 개념 스키마와 외부 스키마 정의
- 나. SDL: 관계 데이터베이스 언어
- 다. VDL: 사용자 뷰를 명시하고 개념 스키마를 정의
- 라. DML: 데이터에 대한 검색, 삽입, 삭제, 수정 등 연산자들의 집합

**정답:** 나

**해설**
SDL은 관계 DB 언어가 아니라 **데이터 저장구조를 정의**하는 언어. 나가 옳지 않음.

---

## 4. (기출 20회) — ALTER 속성 추가

**문제**
고객 테이블을 생성한 후, 주소 속성이 누락되었다. 주소 속성을 추가하기에 적합한 SQL 명령어는?

**보기**
- 가. CREATE
- 나. ALTER
- 다. DROP
- 라. MODIFY

**정답:** 나

**해설**
기존 테이블에 속성 추가 → **ALTER**(보통 ALTER TABLE … ADD).
가(신규 생성), 다(삭제), 라(일부 DBMS 열 수정용·속성 신규 추가에는 ALTER가 적합).

---

# 6강

---

## 1. (기출 14회) — 뷰(View) 사용 목적

**문제**
다음 뷰(View)의 사용 목적에 대한 설명으로 옳지 않은 것은?

**보기**
- 가. 보안 관리를 목적으로 활용할 수 있다.
- 나. 사용상의 편의를 목적으로 활용할 수 있다.
- 다. 수행 속도의 향상을 목적으로 활용할 수 있다.
- 라. 물리적 공간의 효율적 사용을 목적으로 활용할 수 있다.

**정답:** 다

**해설**
뷰는 가상 테이블로 조회 시 기본 테이블 쿼리가 다시 실행되어 **수행 속도 향상 목적이라고 보기 어렵다**(오히려 느려질 수 있음). 다가 옳지 않음.

---

## 2. (기출 14회) — DROP VIEW RESTRICTED

**문제**
기본 테이블 T를 이용하여 뷰 V1을 정의하고 뷰 V1을 이용하여 다시 뷰 V2가 정의되었다. 기본 테이블 T와 뷰 V2를 조인하여 뷰 V3을 정의하였다. 이때 다음과 같은 SQL 문이 실행되면 발생하는 결과를 올바르게 설명한 것은?

```sql
DROP VIEW V1 RESTRICTED ;
```

**보기**
- 가. 하나도 삭제되지 않는다.
- 나. T, V1, V2, V3 모두 삭제된다.
- 다. V1, V2, V3만 삭제된다.
- 라. V1만 삭제된다.

**정답:** 가

**해설**
RESTRICTED는 V1이 다른 뷰(V2·V3)에 **참조되면 삭제하지 않고 오류**. 따라서 하나도 삭제되지 않음.

---

## 3. (기출 14회·21회) — GROUP BY / HAVING

**문제**
은행 각 지점의 계좌들에 들어있는 잔액을 관리하는 데이터베이스가 있다고 할 때, 은행들의 지점별로 평균 잔액을 구하되 평균 잔액이 500,000원을 초과하는 지점들에 대해서만 지점별로 그 평균 잔액을 보여주고자 한다. 이를 SQL 질의로 만들었을 때 빈칸에 들어갈 구분을 옳게 나열한 것은?

```sql
SELECT 지점, avg(잔액)
FROM 계좌
(1) ......... 지점
(2) ......... avg(잔액) > 500000
```

**보기**
- 가. (1) WHERE (2) GROUP
- 나. (1) GROUP (2) HAVING
- 다. (1) GROUP (2) WHERE
- 라. (1) GROUP BY (2) HAVING

**정답:** 라

**해설**
지점별 그룹화 → **GROUP BY**, 집계 결과(`avg`) 조건 → **HAVING**(WHERE 불가).

---

## 4. (기출 15회) — DELETE로 검색결과 만들기

**문제**
DB 테이블을 참고하여 검색결과의 출력을 얻으려면 어떤 SQL을 실행해야 하는가? (기출 15회)

**부서DB(DEPT)**

| DEPT# | DNAME | BUDGET |
|-------|-------|--------|
| D1 | 마케팅부 | 10M |
| D2 | 개발부 | 12M |
| D3 | 연구부 | 5M |

**고용DB(EMP)**

| EMP# | ENAME | DEPT# | SALARY |
|------|-------|-------|--------|
| E1 | 박혜근 | D1 | 40K |
| E2 | 문인재 | D1 | 42K |
| E3 | 안수철 | D2 | 30K |
| E4 | 한수영 | D2 | 35K |

**검색 결과:** EMP에서 E4(한수영) 행만 빠진 결과

**보기**
- 가. `DELETE DEPT where DEPT#="D3"`
- 나. `DELETE DEPT where EMP#="E4"`
- 다. `DELETE EMP where DEPT#="D3"`
- 라. `DELETE EMP where EMP#="E4"`

**정답:** 라

**해설**
검색결과에서 E4만 사라짐 → `DELETE EMP where EMP#="E4"`.
가·다는 결과와 무관, 나는 DEPT에 EMP# 없어 문법 오류.

---

# 7강

---

## 1. (기출 15회) — 뷰 지원이 필요한 이유

**문제**
뷰(Views) 지원이 필요한 이유로 옳지 않은 것은?

**보기**
- 가. 감춰진 데이터에 대한 자동적인 보안을 제공한다.
- 나. 축약 또는 매크로 기능을 제공한다.
- 다. 같은 데이터를 동일한 시간에 다른 여러 사용자들에게 다른 방식으로 보여지도록 한다.
- 라. 논리적 데이터 구조의 변화로부터 사용자와 사용자의 프로그램에 영향을 받도록 제공한다.

**정답:** 라

**해설**
뷰는 논리 구조 변화로부터 사용자·프로그램을 **보호**(데이터 독립성)한다. “영향을 받도록”은 반대 → 라가 옳지 않음.

---

## 2. (기출 15회) — 수정 가능한 뷰

**문제**
다음 생성된 뷰 중에서 deptno 컬럼의 값을 20으로 수정이 가능한 뷰는 무엇인가?

**보기**
- 가.
```sql
CREATE VIEW VW10
AS SELECT empno, ename, deptno FROM emp
WHERE deptno = 10
```
- 나.
```sql
CREATE VIEW VW10
AS SELECT empno, ename, deptno FROM emp
WHERE deptno = 10 WITH CHECK OPTION CONSTRAINT ck1
```
- 다.
```sql
CREATE VIEW VW10
AS SELECT empno, ename, deptno FROM emp
WHERE deptno = 10 WITH READ ONLY
```
- 라.
```sql
CREATE VIEW VW10
AS SELECT deptno, MIN(sal), MAX(sal) FROM emp
GROUP BY deptno
```

**정답:** 가

**해설**
가: CHECK OPTION 없어 기본 테이블 수정 가능(뷰 조건 벗어나도).
나: CHECK OPTION이 deptno=20 갱신 금지. 다: READ ONLY. 라: 집계·GROUP BY 뷰는 보통 갱신 불가.

---

## 3. (기출 16회) — GROUP BY … DESC

**문제**
학생 현황을 조회하려고 할 때 다음 예시된 SQL 구문으로 알 수 없는 것은?

```sql
select 학과번호, 학년, count(*) as '인원수', avg(키) as '평균키'
from 학생 group by 학과번호, 학년 desc
```

**보기**
- 가. 학과와 학년별로 인원수, 평균키를 내림차순으로 출력한다.
- 나. 전체 학생을 소속학과 별로 분류한다.
- 다. 학생 테이블을 검색한다.
- 라. 전체 학생을 소속학과 별로 구분하고, 같은 학과 학생은 다시 학년별로 분류한다.

**정답:** 가

**해설**
`GROUP BY … DESC`의 DESC는 **정렬이 아니라 그룹화 표현**. 내림차순 출력에는 **ORDER BY** 필요. 가의 “내림차순 출력”은 이 SQL로 알 수 없음(성립하지 않음).

---

## 4. (기출 16회) — 뷰의 특징

**문제**
뷰의 특징으로 올바르지 않은 것은?

**보기**
- 가. 뷰가 정의된 기본 테이블이 변경되면, 뷰도 자동적으로 변경된다.
- 나. 외부 스키마는 뷰와 기본 테이블 정의로 구성된다.
- 다. 뷰의 정의는 ALTER 문을 이용해 변경할 수 있다.
- 라. 뷰는 CREATE문에 의해 정의되며, DROP문에 의해 삭제된다.

**정답:** 다

**해설**
뷰 정의는 보통 **ALTER로 직접 변경 불가**. DROP 후 CREATE로 재정의. 다가 옳지 않음.

---

# 8강

---

## 1. (기출 17회) — EMPLOYEE 구매팀 검색

**문제**
다음 표의 `<EMPLOYEE>` 테이블에서 부서가 '구매팀'인 직원 이름과 입사 일자를 검색하기 위한 SQL문으로 적절한 것은?

| 사번 | 이름 | 생년월일 | 입사일자 | 부서 |
|------|------|----------|----------|------|
| 20160009 | 강호동 | 890719 | 2016.5.17 | 개발팀 |
| 20140243 | 나영석 | 850114 | 2014.11.13 | 구매팀 |
| 20110087 | 김수영 | 650524 | 2011.3.24 | 홍보팀 |
| 20170005 | 장재혁 | 911214 | 2017.2.19 | 재무팀 |

**보기**
- 가. `SELECT 이름, 입사일자 FROM EMPLOYEE WHERE 부서 = '구매팀'`
- 나. `SELECT 이름, 입사일자 FROM EMPLOYEE WHERE 구매팀 = '부서'`
- 다. `SELECT 이름, 생년월일 FROM EMPLOYEE WHERE 부서 = '구매팀'`
- 라. `SELECT 이름, 입사일자 FROM EMPLOYEE WHERE 부서 LIKE '%%'`

**정답:** 가

**해설**
가: 조건·컬럼이 요구사항과 일치. 나: WHERE 조건 좌우 오류. 다: 생년월일 조회(요구는 입사일자). 라: 모든 부서 매칭.

---

## 2. (기출 17회·21회) — 뷰 활용 목적

**문제**
한 개 이상의 베이스 테이블로부터 유도되어 만들어진 가상 테이블을 뷰라고 하는데 뷰의 활용 목적으로 적절하지 않은 것은?

**보기**
- 가. 사용상의 편의 목적
- 나. 수행속도의 향상의 목적
- 다. 보안관리의 목적
- 라. 영구적인 작업을 위한 목적

**정답:** 라

**해설**
뷰는 가상 테이블로 실제 데이터를 저장하지 않아 **영구 작업** 목적에 적합하지 않음. 라가 적절하지 않음.

---

## 3. (기출 18회) — 나이 ≤ 35 사원명

**문제**
"나이가 35 이하인 사원의 사원명을 검색하라"라는 질의를 SQL 문으로 표현한 것은?

**보기**
- 가. `SELECT 사번, 사원명 FROM 사원 WHERE 나이 <= 35;`
- 나. `SELECT 사원명 FROM 사원 WHERE 나이 <= 35;`
- 다. `SELECT 사번, 사원명 FROM 사원 WHERE 나이 >= 35;`
- 라. `SELECT 사원명 FROM 사원 WHERE 나이 > 35;`

**정답:** 나

**해설**
요구는 **사원명만**, 조건은 **나이 ≤ 35**. 가는 사번 포함, 다·라는 조건 오류.

---

## 4. (기출 18회) — 서브쿼리 결과

**문제**
다음 학생 테이블과 성적 테이블을 이용하여 아래의 SQL 명령문을 실행했을 때의 결과는?

```sql
SELECT 학점 FROM 성적
WHERE 학번 = ( SELECT 학번 FROM 학생 WHERE 성명 = '강감찬' );
```

**학생**

| 성명 | 학과 | 학번 |
|------|------|------|
| 홍길동 | 컴퓨터 | 1325 |
| 을지문덕 | 전자계산 | 1245 |
| 김유신 | 기계공학 | 1363 |
| 강감찬 | 전자공학 | 1078 |

**성적**

| 학번 | 학점 |
|------|------|
| 1078 | C |
| 1245 | A |
| 1325 | D |
| 1363 | B |

**보기**
- 가. C
- 나. 1078
- 다. 1363
- 라. B

**정답:** 가

**해설**
내부: 강감찬 학번=1078 → 외부: 학점 **C**.

---

## 5. (기출 18회) — SELECT … FROM 빈칸

**문제**
다음의 ( ) 안에 들어갈 적당한 SQL 문장을 고르시오.

`SELECT 이름 ( ) 사원테이블 WHERE 부서 = '총무'`

**보기**
- 가. WITH
- 나. SET
- 다. FROM
- 라. AND

**정답:** 다

**해설**
테이블 지정 키워드 = **FROM**. `SELECT 이름 FROM 사원테이블 WHERE 부서 = '총무'`

---

# 9강

---

## 1. (기출 18회) — 뷰(View) 설명

**문제**
다음 중 뷰(View)에 대한 설명이 아닌 것을 고르시오.

**보기**
- 가. 여러 사용자의 상이한 응용이나 요구를 지원해 준다.
- 나. 물리적인 테이블로 관리가 편하다.
- 다. 사용자가 데이터 관리를 간단히 할 수 있도록 한다.
- 라. 숨겨진 데이터를 위한 자동 보안이 제공된다.

**정답:** 나

**해설**
뷰는 **가상 테이블**로 물리적으로 데이터를 저장하지 않음. “물리적인 테이블로 관리가 편하다”는 설명이 아님.

---

## 2. (기출 19회) — SQL 질의 구조

**문제**
다음 중 아래의 SQL 질의에 대한 설명으로 올바른 질의수행 코드는 무엇인가?

**보기**
- 가. `SELECT <속성리스트> FROM <테이블리스트> [WHERE <조건식>] [GROUP BY <그룹화속성리스트>] [ORDER BY <정렬속성리스트>]`
- 나. `SELECT <테이블리스트> FROM <속성리스트> [WHERE <조건식>] [GROUP BY <그룹화속성리스트>] [ORDER BY <정렬속성리스트>]`
- 다. `SELECT <속성리스트> FROM <정렬속성리스트> [WHERE <조건식>] [GROUP BY <그룹화속성리스트>] [ORDER BY <테이블리스트>]`
- 라. `SELECT <속성리스트> FROM <테이블리스트> [WHERE <그룹화속성리스트>] [GROUP BY <조건식>] [ORDER BY <정렬속성리스트>]`

**정답:** 가

**해설**
표준 절 순서: SELECT 속성 → FROM 테이블 → WHERE → GROUP BY → ORDER BY. 나·다·라는 절/리스트가 뒤바뀜.

---

## 3. (기출 20회) — 최고 월급 사원 SQL

**문제**
emp 테이블은 사원정보를 저장하고 ename, job, sal 애트리뷰트는 사원이름, 직책, 월급을 나타낸다. 사원들 중에서 가장 월급을 많이 받는 사원을 구하는 SQL로 알맞은 것은?

**보기**
- 가. `SELECT ename, MAX(sal) FROM emp`
- 나. `SELECT ename, sal FROM emp HAVING sal = MAX(sal)`
- 다. `SELECT ename, sal FROM emp WHERE sal = MAX(sal)`
- 라. `SELECT ename, sal FROM emp WHERE sal = (SELECT MAX(sal) FROM emp)`

**정답:** 라

**해설**
WHERE에 집계 직접 사용 불가 → 서브쿼리로 MAX(sal) 구한 뒤 비교. 라가 적합.

---

## 4. (기출 21회) — BCNF

**문제**
다음 조건을 모두 만족하는 정규형은?

- 테이블 R에 속한 모든 도메인이 원자값만으로 구성되어 있다.
- 테이블 R에서 키가 아닌 모든 필드가 키에 대해 함수적으로 종속되며, 키의 부분 집합이 결정자가 되는 부분 종속이 존재하지 않는다.
- 테이블 R에 존재하는 모든 함수적 종속에서 결정자가 후보키이다.

**보기**
- 가. BCNF
- 나. 제1정규형
- 다. 제2정규형
- 라. 제3정규형

**정답:** 가

**해설**
원자값(1NF)+부분종속 없음(2NF)+결정자가 후보키(BCNF, 3NF보다 강함) → **BCNF**.

---

# 10강

---

## 1. (기출 17회) — DCL 유형에 해당하지 않는 것

**문제**
데이터 보안, 무결성, 데이터 회복, 병행수행 제어 등을 정의하는데 사용하는 데이터 제어어(DCL)의 유형에 해당하지 않는 것은 무엇인가? (기출 17회)

**보기**
- 가. COMMIT
- 나. ALTER
- 다. ROLLBACK
- 라. REVOKE

**정답:** 나

**해설**
ALTER는 테이블·DB 구조 변경용 **DDL**이다. COMMIT·ROLLBACK·REVOKE는 DCL에 해당.

---

## 2. (기출 14회) — 블라인드 SQL 인젝션

**문제**
SQL인젝션 공격은 DB를 공격해서 정보를 탈취하는 방식이다. 이런 SQL인젝션 공격 중에서 원하는 데이터를 가져올 쿼리를 삽입하여 쿼리의 결과가 참일 때와 거짓일 때의 서버 반응으로 데이터를 얻어내는 기술은 무엇인가? (기출 14회)

**보기**
- 가. 웹기반 인젝션
- 나. 오류기반 인젝션
- 다. 블라인드 인젝션
- 라. 자동 응용 인젝션

**정답:** 다

**해설**
서버가 결과·오류를 직접 반환하지 않을 때 참/거짓 반응 차이로 데이터를 추론하는 기법이 **블라인드 인젝션**.

---

## 3. (기출 15회) — SQL 인젝션 방법 중 옳지 않은 것

**문제**
SQL 인젝션은 데이터베이스에 질의를 수행하는 과정에 악의적인 의도가 있는 구문을 함께 입력시켜 공격자가 원하는 SQL문을 실행시키는 웹 해킹방법이다. 다음 SQL 인젝션 방법에 대한 설명 중 옳지 않은 것은? (기출 15회)

**보기**
- 가. 우회 인증하는 방법은 로그인 인증 쿼리문이 항상 TRUE 결과값이 나오도록 하여 인증 과정을 무력화시키는 작업이다.
- 나. 의도적으로 웹상의 공격 포인트에 SQL을 삽입시켜 에러를 유발 시키고 에러 정보를 통하여 추가적인 정보를 획득한다.
- 다. HAVING/GROUP BY 절을 이용하여 에러를 발생시켜 SELECT 절의 컬럼들의 이름을 알 수 있다.
- 라. INTERSECT 연산자를 이용하여 공격자가 의도한 질의를 파라미터에 붙임으로 원하는 내부 정보를 알 수 있다.

**정답:** 라

**해설**
인젝션에 흔히 쓰는 것은 UNION·OR/AND 등이며, **INTERSECT**로 내부 정보를 얻는다는 설명은 옳지 않음.

---

## 4. (기출 16회) — SQL 인젝션 공격·방어 설명 중 옳지 않은 것

**문제**
데이터베이스 웹 응용에서 로그인시 사용자로부터 아이디와 비밀번호를 입력받아서 다음과 같은 SQL을 실행한다고 할 때 이에 대한 SQL 인젝션 공격법 및 방어법에 대한 설명으로 옳지 않은 것은? (기출 16회)

```sql
SELECT user FROM user_table WHERE id='사용자 입력 아이디' AND password='사용자 입력 비밀번호'
```

**보기**
- 가. SQL 인젝션 공격법은 아이디를 admin이라고 입력하고 비밀번호를 입력하는 칸에 `'OR '1'='1` 이라고 입력한다. (`'` 는 입력 칸을 둘러싸는 구분자이다.)
- 나. SQL 인젝션 공격법은 아이디를 id 라고 입력하고 비밀번호를 입력하는 칸에 `password OR TRUE` 이라고 입력한다. (`'` 는 입력 칸을 둘러싸는 구분자이다.)
- 다. SQL 인젝션 공격법을 방지하기 위한 방법은 위와 같은 SQL 구문에 대하여 데이터베이스의 prepared statement를 사용하는 것이다.
- 라. SQL 인젝션 공격법은 아이디를 user라고 입력하고 비밀번호를 입력하는 칸에 `Hello'); DROP TABLE user_table;--` 이라고 입력한다. (`--` 는 주석을 나타내는 구분자이다.)

**정답:** 나

**해설**
`password OR TRUE` 형태는 해당 쿼리 맥락의 전형적인 인젝션이 아님. 가·라는 전형적 우회·스택드 쿼리, 다는 prepared statement 방어로 타당.

---

## 5. (기출 20회) — SQL 인젝션 대응으로 적절하지 않은 것

**문제**
웹 애플리케이션의 허점을 악용하여 해커들이 비정상적인 SQL 구문을 실행하게 하는 것을 SQL 인젝션(Injection)이라 한다. 다음 중 SQL 인젝션 공격을 차단하기 위한 대응 방법으로 적절하지 않은 것은? (기출 20회)

**보기**
- 가. 데이터베이스의 에러를 노출하여 SQL 인젝션 공격을 무력화한다.
- 나. 웹 방화벽(WAF: Web Application Firewall)을 강화한다.
- 다. 매개변수화된 쿼리를 사용하여 운영코드와 데이터를 분리하고 상호작용이 발생하지 않도록 한다.
- 라. 데이터베이스에 대한 접근 권한을 축소하여 잠재적인 피해를 최소화한다.

**정답:** 가

**해설**
에러 노출은 공격자에게 구조·취약점 정보를 주므로 대응이 아니라 **위험**. WAF·매개변수화 쿼리·최소 권한이 올바른 대응.

==========

# 11강

---

## 1. (기출 14회·16회·20회) — 트랜잭션 ACID 특성이 아닌 것

**문제**
데이터베이스에서 하나의 논리적 작업 단위를 구성하는 일련의 연산들의 집합을 트랜잭션이라고 한다. 다음 중 트랜잭션의 ACID 특성이 아닌 것은? (기출 14회, 16회, 20회)

**보기**
- 가. Atomicity(원자성)
- 나. Consistency(일관성)
- 다. Integrity(무결성)
- 라. Durability(지속성)

**정답:** 다

**해설**
ACID는 Atomicity·Consistency·**Isolation**(고립성)·Durability이다. Integrity(무결성)는 DB의 중요 성질이지만 ACID 구성 요소가 아님.

---

## 2. (기출 21회) — MSSQL 로그 파일

**문제**
MSSQL 데이터베이스의 로그(log) 파일에 관한 설명으로 가장 올바른 것은? (기출 21회)

**보기**
- 가. 현재 로그가 기록되는 파일명은 errorlog.1 부터 시작하며 errorlog.6 까지 총 6개의 로그파일을 가진다.
- 나. 로그인과 로그아웃이 어떤 IP에서 이루어졌는지 정보가 기록된다.
- 다. 윈도우 이벤트 로그와 같은 폴더에 저장되며 바이너리 형식이다.
- 라. DB의 모든 error 로그 기록은 DB자체의 errorlog 파일들에만 기록된다.

**정답:** 나

**해설**
MSSQL 오류로그에는 로그인·로그아웃 및 해당 **IP**가 기록된다. 가(개수 고정·시작명 오류), 다(텍스트·별도 LOG 폴더), 라(윈도우 이벤트에도 일부 기록)는 옳지 않음.

---

## 3. (기출 21회) — DB 파일 확장자 짝이 옳지 않은 것

**문제**
디지털 포렌식 전문가 甲은 압수수색 현장에서 데이터베이스의 파일 시스템 레벨에서 백업된 파일을 찾고 있다. 다음 중 각각의 파일 확장자 그룹과 짝지어진 데이터베이스의 연결이 옳지 않은 것은? (기출 21회)

**보기**
- 가. `*.MYD`, `*.MYI`, `*.FRM` — MariaDB
- 나. `*.MDB`, `*.ACCDB` — MySQL
- 다. `*.MDF`, `*.LDF`, `*.NDF` — MS SQL Server
- 라. `*.CTL`, `*.DBF`, `*.LOG` — Oracle

**정답:** 나

**해설**
`*.MDB`/`*.ACCDB`는 **Microsoft Access** 확장자이다. MySQL/MariaDB는 MYD·MYI·FRM 등을 사용.

---

## 4. (기출 21회) — OLAP 요건으로 옳지 않은 것

**문제**
OLAP(Online analytical processing) 시스템이 갖추어야 할 요건으로 가장 옳지 않은 것은? (기출 21회)

**보기**
- 가. 사용자는 OLAP 데이터에 대해 다차원 뷰를 가질 수 있어야 한다.
- 나. 사용자는 차원의 수에 관계없이 같은 연산을 실행시킬 수 있어야 한다.
- 다. 주제 지향적이고, 통합적이고, 비소멸성이며, 시간에 따라 변화하는 데이터베이스로서 의사결정 지원을 제공해야 한다.
- 라. 차원의 수가 증가하더라도 검색 성능이 저하되어서는 안 된다.

**정답:** 다

**해설**
다의 정의(주제 지향·통합·비소멸·시간 변이)는 **데이터 웨어하우스** 특성이지 OLAP 시스템 요건이 아님.

---

## 5. (기출 15회) — 데이터베이스 암호화 방식 중 옳지 않은 것

**문제**
개인정보를 처리하고 관리하는 기업은 관련 법률에 따라 저장된 개인정보를 데이터베이스에 암호화하여 저장함으로써 개인정보의 변조, 파괴 및 유출을 방지해야 한다. 데이터베이스 암호화 방식에 대한 설명 중 옳지 않은 것은? (기출 15회)

**보기**
- 가. DB 서버 암호화 방식 : 암·복호화 모듈이 DB 서버에 설치되고 DB 서버에서 암·복호화 모듈을 호출하는 방식
- 나. DBMS 자체 암호화 방식 : DBMS에 내장되어 있는 암호화 기능을 이용하여 암·복호화 처리를 수행하는 방식
- 다. DBMS 암호화 기능 호출방식 : DB 서버의 DBMS 커널이 애플리케이션 서버의 암·복호화 API를 호출하는 방식
- 라. 운영체제 암호화 방식 : OS에서 발생하는 물리적인 입출력을 이용한 암·복호화 방식으로 DBMS의 데이터 파일 암호화

**정답:** 다

**해설**
DBMS 커널이 애플리케이션 서버의 암·복호화 API를 호출한다는 설명은 옳지 않음. DBMS는 자체 암호화로 처리해야 하며 앱 서버 호출 관계는 주요 방식이 아님.

==========

# 12강

---

## 1. (기출 16회) — SQLite 설명 중 옳지 않은 것

**문제**
SQLite 데이터베이스에 관한 설명 중 옳지 않은 것은? (기출 16회)

**보기**
- 가. Rollback journal 모드에서 최신 데이터는 .db-journal, 이전 데이터는 .db에서 발견할 수 있다.
- 나. 컬럼을 삭제하거나 변경하는 것이 제한된다.
- 다. WAL 모드에서 최신의 데이터는 항상 .db-wal 파일에 있다.
- 라. 디지털포렌식과 관련된 레코드 데이터는 리프(leaf) 페이지에 존재하고, 인터널(Internal) 페이지는 리프 페이지까지의 연결고리를 제공할 뿐 실제 레코드 데이터는 포함하지 않는다.

**정답:** 가

**해설**
Rollback journal에서 최신 데이터는 **.db**에 반영되고 .db-journal은 트랜잭션 중 임시 파일(성공 후 삭제)이다.

---

## 2. (기출 16회) — 포렌식 증거수집 파일·설명 짝

**문제**
데이터베이스 포렌식에서 증거수집 파일 목록으로, 특정 로그 및 설정 제어 파일과 설명이 올바르게 짝지어진 것은? (기출 16회)

**보기**
- 가. Oracle - CTL - 데이터베이스 마운트/오픈 시에 사용하는 파일
- 나. MySQL - binlog - 테이블 구조에 대한 정보를 담는 파일
- 다. MSSQL - MDF - 데이터베이스 사용, 오류등을 기록하는 로그 파일
- 라. Oracle - Log - 데이터베이스를 백업하는 파일

**정답:** 가

**해설**
Oracle CTL(컨트롤 파일)은 마운트/오픈에 필수. binlog는 변경·트랜잭션(구조 아님), MDF는 데이터 파일(로그는 LDF), Oracle Log는 주로 Redo(백업 파일 아님).

---

## 3. (기출 17회) — 오라클 분석 수집 파일이 아닌 것

**문제**
오라클 데이터베이스 서버 분석을 위해 수집하는 파일의 종류가 아닌 것은 무엇인가? (기출 17회)

**보기**
- 가. *.ibdata
- 나. *.dbf
- 다. *.ctl
- 라. *.log

**정답:** 가

**해설**
`*.ibdata`는 **MySQL InnoDB** 데이터 파일. Oracle은 dbf·ctl·log(redo 등)를 수집.

---

## 4. (기출 18회) — 리스너 로그로 알 수 없는 것

**문제**
오라클 포렌식에서 리스너 로그 파일 분석을 통해 알 수 없는 것은? (기출 18회)

**보기**
- 가. 접속한 날짜와 시간
- 나. 접속한 컴퓨터 이름과 계정
- 다. 침해사고 시 악성코드의 이름과 공격유형
- 라. 접속한 IP 주소와 포트 번호

**정답:** 다

**해설**
리스너 로그는 접속 시각·클라이언트명·계정·IP·포트 등을 남기지만 **악성코드명·공격유형**은 기록하지 않음.

==========

# 13강

---

## 1. (기출 18회) — 원격 접속 기본 포트 짝이 적절하지 않은 것

**문제**
디지털 포렌식 전문가 甲은 원격지에서 데이터베이스 서버에 접속하여 데이터를 분석하는 것을 허용하도록 방화벽(Firewall) 구성을 변경하려고 한다. 다음 중 각각의 원격 접속 기본 포트 번호에 해당하는 데이터베이스 목록 중 적절하지 않은 것은? (기출 18회)

**보기**
- 가. 3389/TCP - Microsoft SQL Server
- 나. 1521/TCP - Oracle
- 다. 3306/TCP - MySQL
- 라. 5432/TCP - PostgreSQL

**정답:** 가

**해설**
3389/TCP는 **RDP** 기본 포트. MS SQL Server 원격은 **1433/TCP**.

---

## 2. (기출 19회·20회) — 데이터베이스 보안 솔루션 잘못된 설명

**문제**
데이터베이스 보안 솔루션에 대한 설명으로 잘못된 것은? (기출 19회, 20회)

**보기**
- 가. 데이터베이스 암호화 솔루션은 Plug In 방식과 API 방식으로 분류되며 Plug In 방식은 데이터베이스 서버에 별도의 암호화 솔루션을 설치하고 애플리케이션이 데이터 조작어를 실행하면 암호화하거나 복호화하는 방식이다.
- 나. 데이터베이스 감사 솔루션은 데이터베이스의 모든 접근에 대해서 로그를 기록하여 추적성을 확보하는 것으로, SQL을 실행할 때 그 로그를 모두 기록하고 관리하는 솔루션이다.
- 다. 데이터베이스 접근제어 솔루션은 권한이 없는 사용자가 데이터베이스 접근을 시도할 때 이를 차단하는 것으로, 프록시 방식은 이중화된 네트워크 포트를 통해 데이터베이스에 접근하는 것을 말한다.
- 라. 데이터베이스 암호화는 데이터의 내용을 암호화함으로써 불법적으로 데이터 취득 시 해독이 불가능하게 하기 위함이다.

**정답:** 다

**해설**
프록시 방식은 중간 서버를 통한 접근 제어·모니터링이지, **이중화된 네트워크 포트**와 직접 관련된다는 설명은 잘못됨.

---

## 3. (기출 19회) — DB 구축 시 고려사항으로 거리가 먼 것

**문제**
데이터베이스를 구축할 때 고려해야 할 사항으로 가장 거리가 먼 것은? (기출 19회)

**보기**
- 가. 운영체제의 관리자 계정과 DB의 관리자 계정 및 소속 그룹은 독립적으로 디자인되어야 한다.
- 나. DBMS의 직접적인 설정을 제어하거나 운영에 필수적인 파일들을 대상으로 변조를 감지하기 위하여 수시로 checksum을 검증한다.
- 다. 데이터베이스 내에 생성되는 테이블, 뷰, 컬럼들과 같은 객체의 이름은 최대한 이름을 기반으로 추측하기 쉽도록 명명해야 한다.
- 라. DB 서버의 네트워크 인터페이스에서는 데이터베이스 자체 또는 운영체제에서 제공하는 기능을 사용하여 IP 주소나 MAC 주소를 기반으로 하는 필터링을 구현해야 한다.

**정답:** 다

**해설**
객체 이름을 쉽게 추측되게 짓는 것은 보안상 **불리**. 추측이 어렵게 명명하는 편이 안전하다.

---

## 4. (기출 20회) — 괄호 안 암호화 방식 (TDE)

**문제**
개인정보를 처리하고 관리하는 기업의 경우 관련 법률에 따라 저장된 개인정보를 데이터베이스에 암호화하여 저장함으로써 개인정보의 변경, 파괴 및 유출을 방지해야 한다. 개인정보 처리시스템의 데이터베이스를 암-복호화할 수 있는 방식은 암-복호화 모듈의 위치와 암-복호화 모듈의 요청 위치의 조합에 따라 구분할 수 있다. 다음 중 아래의 괄호 안에 들어갈 알맞은 암호화 방식은? (기출 20회)

( ) 방식은 DBMS에 내장되어 있는 암호화 기능(TDE: Transparent Data Encryption)을 이용하여 암-복호화 처리를 수행하는 방식이다. DBMS 커널 수준에서 암-복호화를 수행하므로 DB 서버의 CPU, 주기억장치, 디스크 등의 추가적인 부하가 적으며, 기존 응용프로그램의 수정이나 DB 스키마의 변경이 거의 필요하지 않고 DBMS 엔진에 최적화된 성능을 제공할 수 있다.

**보기**
- 가. 응용프로그램 자체 암호화
- 나. 디스크 암호화
- 다. DBMS 자체 암호화
- 라. 운영체제 암호화

**정답:** 다

**해설**
TDE 등 DBMS 내장·커널 수준 암복호화 → **DBMS 자체 암호화**.

---

## 5. (기출 20회·21회) — MySQL 서버 보안 설정으로 옳지 않은 것

**문제**
MySQL 서버 보안 설정으로 옳지 않은 것은? (기출 20회, 21회)

**보기**
- 가. 디폴트 test 데이터베이스를 삭제한다.
- 나. 사용하는 운영체제 및 데이터 디렉터리의 설정에 따라 skip-symbolic-links 옵션을 사용하여 심볼릭 링크의 사용을 중지해야 한다.
- 다. 비사용 및 불필요한 유저 정의 함수를 삭제한다.
- 라. 원격 접속이 필요 없는 환경일 경우, TCP/IP 연결을 활성화한다.

**정답:** 라

**해설**
원격이 불필요하면 TCP/IP는 **비활성화**해야 한다. 활성화는 옳지 않은 설정.

==========

# 14강

---

## 1. (기출 15회) — 비식별화 데이터 활용 중 옳지 않은 것

**문제**
수집된 데이터가 개인정보로 판별이 되고 식별자를 가지고 있다면 데이터 활용 및 배포를 위해서는 비식별 조치를 수행해야 한다. 비식별 조치가 수행된 데이터들은 평가를 통해 그 정도가 적정하다고 판단이 되면 데이터 활용이 가능하다. 다음 비식별화 조치가 수행된 데이터의 활용에 대한 설명으로 옳지 않은 것은? (기출 15회)

**보기**
- 가. 제3자에게 제공할 수 있지만, 불특정 다수에게 공개하는 것은 원칙적으로 금지한다.
- 나. 재식별의 가능성이 매우 높은 빅데이터 분석에는 사용할 수 없다.
- 다. 데이터 이용 목적을 달성하거나 해당 데이터가 불필요하게 된 경우에는 지체 없이 파기 조치한다.
- 라. 데이터 활용 과정에서도 지속적으로 사후관리 조치사항을 준수하여야 한다.

**정답:** 나

**해설**
나가 옳지 않은 설명으로 제시됨.

---

## 2. (기출 16회) — 삭제 테이블 복구 실행파일

**문제**
다음 사례에서 포렌식조사관이 MySQL 데이터베이스에서 삭제된 테이블을 복구하는 데 사용한 실행파일로서 옳은 것은? (기출 16회)

포렌식 조사관이 데이터베이스 증거수집을 위해 OO기업을 방문하였다. 현장을 살펴보니 Linux 기반 MySQL 데이터베이스 서버가 운영되고 있었다. 그런데 데이터베이스 관리자가 조사를 방해하려고 특정 테이블(Table)을 고의로 삭제한 정황이 발견되었다. 하지만, 데이터베이스 생성 당시부터의 로그가 모두 보관되어 있어 포렌식 조사관은 삭제된 테이블을 복구할 수 있었다.

**보기**
- 가. mysqldump
- 나. mysqlbinlog
- 다. mysqld
- 라. mysqld_safe

**정답:** 나

**해설**
바이너리 로그 분석·추출에 **mysqlbinlog**를 사용.

---

## 3. (기출 16회) — DBMS와 백업 명령어 연결이 옳지 않은 것

**문제**
증거를 수집하기 위해 데이터베이스를 백업 하고자 한다. 다음 중 DBMS(Database Management System)와 백업 명령어의 연결이 옳지 않은 것은? (기출 16회)

**보기**
- 가. Oracle - dump database
- 나. Mssql - backup database
- 다. Mysql - mysqldump
- 라. Sybase - dump database

**정답:** 가

**해설**
Oracle에는 `dump database`가 없고 **exp/expdp**를 사용.

---

## 4. (기출 16회) — 운영 중 물리 백업

**문제**
다음 중에서 운영 중인 데이터베이스의 파일을 복사하는 물리적 백업 방법은 무엇인가? (기출 16회)

**보기**
- 가. DUMP
- 나. COLD BACKUP
- 다. HOT BACKUP
- 라. SNAPSHOT

**정답:** 다

**해설**
운영(온라인) 중 파일 복사 물리 백업은 **HOT BACKUP**. COLD는 정지 후, DUMP는 주로 논리 백업.

---

## 5. (기출 17회) — 데이터베이스 증거 수집 절차 순서

**문제**
데이터베이스 증거 수집을 위한 절차에 알맞은 것은 무엇인가? (기출 17회)

① 현장을 통제하고 보존한다.
② 현장 확인 및 분석을 한다.
③ 관계자 협조요청 및 입회인을 참관토록 한다.
④ 디지털 증거수집 방법을 결정한다.
⑤ 디지털 증거 수집 대상을 확인한다.

**보기**
- 가. ① - ② - ③ - ④ - ⑤
- 나. ① - ③ - ② - ⑤ - ④
- 다. ② - ③ - ① - ④ - ⑤
- 라. ② - ① - ③ - ④ - ⑤

**정답:** 나

**해설**
통제·보존 → 협조·입회 → 현장 확인·분석 → 수집 대상 확인 → 수집 방법 결정.

---

## 6. (기출 17회) — MySQL 백업 유틸리티

**문제**
MySQL 데이터베이스에서 백업을 위한 유틸리티는 무엇인가? (기출 17회)

**보기**
- 가. mysqldump
- 나. mysqlslap
- 다. mysqlcheck
- 라. myisamlog

**정답:** 가

**해설**
백업용은 **mysqldump**.

---

## 7. (기출 18회) — 백업 필요성이 가장 적은 것

**문제**
디지털 포렌식 전문가 甲은 온라인 가동 중인 데이터베이스 서버 시스템에서 변조된 회계자료를 찾기 위해 증거분석용 자료를 백업하려고 한다. 다음 중 파일을 백업할 필요성이 가장 적은 것은? (기출 18회)

**보기**
- 가. 데이터베이스 데이터 파일
- 나. 데이터베이스 서버 환경설정 파일
- 다. 데이터베이스 공유 라이브러리 및 실행 파일
- 라. 오류 로그 및 트랜잭션 로그 파일

**정답:** 다

**해설**
공유 라이브러리·실행 파일은 데이터 변조 추적과 직접 관련이 적어 우선순위가 가장 낮음.

---

## 8. (기출 18회) — 증거인멸 감지 시 현장 조치로 거리가 먼 것

**문제**
어떤 회사에서 MSSQL Server에 접속하여 증거를 수집중이다. 증거를 수집하는 동안 아래의 내용으로 증거인멸이 시도됨을 감지하였다면 현장에서 바로 조치해야 할 사항으로 가장 거리가 먼 것은? (기출 18회)

회사의 DBA(데이터베이스관리자)는 원격으로 회사의 MSSQL Server에 접속하여 안건과 관련된 테이블을 Drop 한 후, 동일 명칭과 속성을 갖는 빈 테이블을 재생성 해놓음.

**보기**
- 가. 전체 테이블에 대한 생성시간을 조회하여 삭제하고 재생성한 테이블들을 특정한다.
- 나. 동 시간대의 트랜잭션 로그를 확보한 후 이를 통해 삭제 데이터를 특정한다.
- 다. 응용프로그램 로그, 데이터베이스 서버 로그, 네트워크 로그를 확보하여 불법 접근자에 대한 추적에 들어간다.
- 라. 서버를 중지시키고 디스크를 분리한 후 쓰기 방지 장치를 부착하여 삭제 파일 복구 작업에 들어간다.

**정답:** 라

**해설**
즉시 서버 중지·디스크 분리는 과도·업무 중단 위험이 커서 현장 즉시 조치로는 거리가 멀다.

==========

# 15강

---

## 1. (기출 18회·19회) — 오라클 시간 관련 증거

**문제**
오라클 포렌식 분석 시, 시간 관련 증거(Time Based Evidence)에 대한 정확성을 확보하기 위해 함께 검토해야 할 자료로 가장 적절하지 않은 것은? (기출 18회, 19회)

**보기**
- 가. Listener Log
- 나. Sqlnet Log
- 다. Statspack Script
- 라. Sysdba Trace

**정답:** 다

**해설**
Statspack Script는 DB 성능 모니터링용으로 시간 증거와의 직접 관련성이 낮다. Listener/Sqlnet/Sysdba Trace는 접속·통신·관리자 행위의 시간 정보 확보에 유용.

---

## 2. (기출 19회·21회) — 증거수집 절차 순서

**문제**
다음은 데이터베이스 증거수집방법의 절차에 대한 설명이다. 절차를 순서대로 나열하시오? (기출 19회, 21회)

① 수집된 증거물 목록 생성
② 휘발성자료를 DB종류별로 나누어 증거 수집
③ 데이터베이스 서버 동작 여부 확인
④ 데이터베이스 인스턴스 동작 여부 확인
⑤ 증거물 포장 또는 확보

**보기**
- 가. ④ → ③ → ② → ① → ⑤
- 나. ④ → ③ → ② → ⑤ → ①
- 다. ③ → ④ → ② → ① → ⑤
- 라. ③ → ④ → ② → ⑤ → ①

**정답:** 다

**해설**
서버 동작 확인(③) → 인스턴스 동작 확인(④) → 휘발성 자료 수집(②) → 목록 생성(①) → 포장·확보(⑤).

---

## 3. (기출 20회) — 증거자료 수집 우선순위

**문제**
데이터베이스 포렌식 증거자료 수집과정에서 우선순위가 가장 높은 것은? (기출 20회)

**보기**
- 가. 데이터베이스 내용 및 데이터에 의해 만들어진 메타 정보 데이터(metadata)
- 나. 운영체제에 기록되어 있는 System 이벤트 로그
- 다. 접속한 사용자의 IP와 수행중인 SQL문
- 라. 데이터베이스의 변경과 관련된 insert, update, delete 트랜잭션 로그

**정답:** 다

**해설**
접속 IP·수행 중 SQL문은 휘발성이 매우 높아 현재 의심 행위 추적에 최우선. 메타·OS 이벤트·트랜잭션 로그는 상대적으로 낮은 우선순위.

---

## 4. (기출 20회·21회) — MySQL 삭제 테이블 복구 도구

**문제**
다음 사례에서 포렌식 조사관이 MySQL 데이터베이스에서 삭제된 테이블을 복구하는데 사용한 도구로써 옳은 것은? (기출 20회, 21회)

포렌식 조사관이 데이터베이스 증거수집을 위해 OO기업을 방문하였다. 현장을 살펴 보니 Linux 기반 MySQL 데이터베이스 서버가 운영되고 있었다. 그런데 데이터베이스 관리자가 조사를 방해하려고 특정 테이블(Table)을 고의로 삭제한 정황이 발견되었다. 하지만, 데이터베이스 생성 당시부터의 로그가 모두 보관되어 있어 포렌식 조사관은 삭제된 테이블을 복구할 수 있었다.

**보기**
- 가. mysqldump
- 나. mysqlbinlog
- 다. mysqld
- 라. mysqld_safe

**정답:** 나

**해설**
mysqlbinlog로 바이너리 로그(변경 연산)를 읽어 삭제 테이블을 복구. mysqldump는 백업 복원용이며 미백업 삭제분은 불가. mysqld/mysqld_safe는 서버 데몬·기동 스크립트.

---

## 5. (기출 20회) — 로그 분석 절차

**문제**
데이터베이스 포렌식의 로그 분석 절차에 해당하지 않는 것은? (기출 20회)

**보기**
- 가. 데이터베이스 로그를 확인한다.
- 나. 기능 로그의 위치를 확인한다.
- 다. 트랜잭션 로그의 위치 및 트랜잭션 로그를 확인한다.
- 라. 시스템 로그(에러 로그)의 위치 및 시스템 로그(에러 로그)를 확인한다.

**정답:** 나

**해설**
기능 로그는 특정 앱·서비스 기능 동작 기록으로 DB 포렌식 로그 분석의 주된 절차에 해당하지 않음. DB/트랜잭션/시스템(에러) 로그 확인이 해당 절차.

---

==========
