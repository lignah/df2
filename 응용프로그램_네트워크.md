# 응용프로그램과 네트워크

# 1강

---

## 1. (기출 15회) — 웹 브라우저 사용 흔적 (캐시·쿠키)

**문제**
다음 설명을 읽고 질문에 답하시오.

> 조사관 A는 불법 도박 사이트 운영과 관련된 디스크 이미지 증거물을 조사하던 중 불법 도박 사이트의 관리자 페이지 로그인 성공 시 확인할 수 있는 이미지 파일이 증거물에 저장된 것을 확인할 수 있었다. 추가적으로 불법 도박 사이트 관리자의 접속 계정과 패스워드를 확인할 수 있었다.

위와 같은 정보를 확인하기 위해 조사해야 할 웹 브라우저 사용 흔적은 무엇인가?

**보기**
- 가. 관리자 페이지 관련 이미지: 웹 브라우저 캐시 / 관리자 계정 정보: 웹 브라우저 쿠키
- 나. 관리자 페이지 관련 이미지: 웹 브라우저 히스토리 / 관리자 계정 정보: 웹 브라우저 다운로드 목록
- 다. 관리자 페이지 관련 이미지: 웹 브라우저 쿠키 / 관리자 계정 정보: 웹 브라우저 히스토리
- 라. 관리자 페이지 관련 이미지: 웹 브라우저 히스토리 / 관리자 계정 정보: 웹 브라우저 쿠키

**정답:** 가

**해설**
웹 브라우저 **캐시**는 방문 페이지의 이미지·스크립트·CSS 등을 저장하므로 관리자 페이지 관련 이미지는 캐시에서 확인한다. **쿠키**는 로그인·세션 정보를 담을 수 있어 관리자 계정·패스워드 확인에 활용된다. 히스토리는 URL 기록, 다운로드 목록은 파일 다운로드 이력으로 이미지 파일·계정 정보와 직접 매칭되지 않는다.

---

## 2. (기출 14회·17회) — 웹 브라우저 관련 데이터

**문제**
수사관 ‘홍길동’은 증거로 확보한 시스템에서 웹브라우저와 관련된 데이터를 조사하고자 한다. 다음 중 웹브라우저와 관련된 데이터로 거리가 먼 것은?

**보기**
- 가. 쿠키
- 나. 임시 파일
- 다. FAT (File Allocation Table)
- 라. 히스토리

**정답:** 다

**해설**
쿠키·임시 파일(캐시)·히스토리는 웹 브라우저 아티팩트이다. **FAT**은 파일시스템 구조로, 브라우저 사용 흔적과는 거리가 멀다.

---

## 3. (기출 7회) — 캐시

**문제**
웹 사이트 방문 시 빠른 재접속을 위해 이미지 파일 등이 자동으로 저장되는 것으로서 사용자의 특정 행위를 유추할 수 있는 브라우저 정보는?

**보기**
- 가. History 정보
- 나. Cache 정보
- 다. Cookie 정보
- 라. Download List 정보

**정답:** 나

**해설**
이미지 등이 자동 저장되어 재접속을 빠르게 하는 것은 **Cache(캐시)** 이다. History는 방문 URL, Cookie는 세션·설정, Download List는 다운로드 이력이다.

---

---

# 2강

---

## 1. (기출 16회) — IE11 사용흔적 폴더

**문제**
윈도우즈 10에서 인터넷익스플로러 11의 사용흔적을 발견할 수 있는 폴더가 **아닌** 것은?

**보기**
- 가. `C:\Users\%User%\AppData\Local\Microsoft\Windows\Temp`
- 나. `C:\Users\%User%\AppData\Local\Microsoft\Internet Explorer\Recovery\Active`
- 다. `C:\Users\%User%\AppData\LocalLow\Microsoft\Internet Explorer\Services`
- 라. `C:\Users\%User%\AppData\Local\Microsoft\Windows\INetCache`

**정답:** 다

**해설**
가는 임시 데이터, 나는 IE 복구 세션(Active), 라는 INetCache(캐시)로 사용 흔적 조사에 유용하다. **다(Services)** 는 IE 서비스 관련 정보 저장 위치로 사용 흔적과는 거리가 멀다.

---

## 2. (기출 16회) — WebCacheV01.dat

**문제**
다음은 WebCacheV01.dat 파일에 대한 설명이다. **옳지 않은** 것은?

**보기**
- 가. 인터넷 익스플로러 10버전 이상에서 생성되는 파일이다.
- 나. 웹 브라우저의 캐시, 히스토리, 쿠키, 다운로드 흔적이 기록되어 있다.
- 다. 기록주체는 웹 브라우저가 담당하며 별도 작업 없이 다른 디렉토리에 복사하여 분석할 수 있다.
- 라. 저장 위치는 `%USERPROFILE%\AppData\Local\Microsoft\Windows\WebCache` 이다.

**정답:** 다

**해설**
WebCacheV01.dat는 **ESE** 데이터베이스이며 Windows가 관리하는 부분이 있어, 단순 복사만으로는 잠금·손상 등으로 분석이 어려울 수 있다. 복구·잠금 해제 등 **추가 작업**이 필요할 수 있다. 가·나·라는 타당한 서술.

---

## 3. (기출 17회) — IE History

**문제**
Microsoft Internet Explorer의 History에서 알 수 있는 정보가 **아닌** 것은?

**보기**
- 가. 웹 페이지에서 다운로드 하지 않고 바로 실행시킨 파일
- 나. 다운로드 받은 시간
- 다. 마지막 방문 시간
- 라. 접속한 URL

**정답:** 가

**해설**
History는 주로 방문 웹페이지 관련 정보(URL, 마지막 방문 시간, 다운로드 시간 등)를 저장한다. **다운로드하지 않고 바로 실행한 파일** 정보는 History에 남지 않는다.

---

## 4. (기출 15회) — Chrome 시크릿 모드

**문제**
Chrome 웹 브라우저의 시크릿 모드에 대한 설명 중 **옳지 않은** 것은?

**보기**
- 가. 방문하는 사이트는 Chrome에 저장되지 않는다.
- 나. 여러 개의 탭을 열 수 있으며 자유롭게 탭을 오가며 여러 페이지를 확인할 수 있으나, 탭을 닫으면 방문한 사이트 정보는 Chrome에서 저장하지 않는다.
- 다. 다운로드하는 파일의 기록을 저장하지 않으나, 다운로드한 파일은 다른 사용자가 이 컴퓨터에서 파일을 열어 볼 수 있다.
- 라. 서비스 제공업체, 회사 고용주, 방문한 웹사이트도 누구도 인터넷 사용 기록을 확인할 수 없다.

**정답:** 라

**해설**
시크릿 모드는 **로컬** 브라우저 기록을 남기지 않을 뿐, ISP·고용주·방문 사이트는 여전히 활동을 추적·기록할 수 있다. “누구도 확인할 수 없다”는 서술은 옳지 않다. 가·나·다는 타당한 설명.

---

---

# 3강

---

## 1. (기출 19회) — 이메일 헤더 설명

**문제**
다음 중 보기에서 이메일 헤더에 대한 설명으로 옳은 것을 모두 고른 것은?

> ㄱ. 이메일 메시지 자체를 담고 있는 부분이다.
> ㄴ. 송신자가 수신자에게 이메일을 보낼 때 거치는 경로 정보를 저장한다.
> ㄷ. 이메일 서버가 할당한 고유 번호인 메시지 ID를 담고 있다.

**보기**
- 가. ㄱ
- 나. ㄴ
- 다. ㄱ, ㄴ
- 라. ㄴ, ㄷ

**정답:** 라

**해설**
ㄱ은 본문(Body)에 대한 설명으로 헤더가 아니다. ㄴ(경로/Received)·ㄷ(Message-ID)은 헤더에 해당한다.

---

## 2. (기출 15회) — 이메일 헤더 message-ID

**문제**
이메일 메시지를 추적할 때에는 로그에 크게 의존한다. 이것은 이메일 경로에 있는 각 서버가 메시지 헤더에 추가하는 정보 중의 하나이다. 이메일 서버가 할당하는 고유번호인 이것과 서버에 저장되어 있는 로그의 상관관계를 확인하면 특정 컴퓨터에서 메시지가 송수신 되었다는 것을 증명할 수 있게 된다. 이것은 무엇인가?

**보기**
- 가. message-ID
- 나. delivered-to
- 다. received-ID
- 라. return-path

**정답:** 가

**해설**
**Message-ID**는 이메일 서버가 메시지에 부여하는 고유 식별자로 헤더에 포함되며, 서버 로그와 대조해 송수신을 입증하는 데 핵심이다. delivered-to·return-path는 다른 용도이고 received-ID는 표준 헤더 항이 아니다.

---

## 3. (기출 14회·19회) — 이메일 클라이언트 파일 형식

**문제**
이메일 클라이언트가 저장하는 파일 형식이 **아닌** 것은?

**보기**
- 가. EML
- 나. UML
- 다. PST
- 라. DBX

**정답:** 나

**해설**
EML(개별 메일), PST(Outlook), DBX(Outlook Express)는 이메일 클라이언트 저장 형식이다. **UML**은 소프트웨어 모델링 언어로 이메일 클라이언트 파일 형식이 아니다.

---

## 4. (기출 18회) — 이메일 클라이언트

**문제**
다음 중 이메일 클라이언트에 대한 설명이 **아닌** 것은?

**보기**
- 가. 대부분의 이메일 클라이언트가 지원하는 중요한 프로토콜은 MIME이다.
- 나. MIME은 이메일 클라이언트를 통해 이진 첨부파일을 보낼 때 쓰인다.
- 다. MAPI는 Microsoft Outlook 클라이언트와 연동한다.
- 라. 대부분의 이메일 클라이언트는 이메일 전송에 POP3 프로토콜을 사용한다.

**정답:** 라

**해설**
이메일 **전송**에는 **SMTP**를 사용한다. POP3는 서버에서 메일을 **수신(다운로드)** 하는 프로토콜이다. 가·나(MIME)·다(MAPI)는 타당한 서술.

---

## 5. (기출 14회) — SMTP와 POP

**문제**
SMTP와 POP에 대한 설명으로 **옳지 않은** 것은?

**보기**
- 가. SMTP는 메일 서버 간의 이메일을 전송하는데 사용된다.
- 나. POP은 사용자가 메일 서버에서 이메일을 가져오는데 사용된다.
- 다. POP은 기본적으로 메일을 로컬과 서버에 동시에 저장해두는 방식이다.
- 라. SMTP는 일반적으로 TCP 포트번호 25번을 사용한다.

**정답:** 다

**해설**
POP은 기본적으로 서버에서 로컬로 다운로드한 뒤 **서버에서 삭제**하는 방식이다(설정으로 서버 보관 가능하나 기본은 아님). “로컬과 서버에 동시 저장이 기본”이라는 서술은 옳지 않다.

---

## 6. (기출 17회) — POP3 비동기화·포트

**문제**
다음은 이메일 POP3 프로토콜에 관한 설명이다. 괄호 안에 맞는 것은?

> 사용자가 이메일 서버와 연결하면 다운로드 하지 않은 이메일을 서버가 모두 보내주는 ( A ) 방식을 사용하며, ( B ) 포트번호를 사용한다.

**보기**
- 가. A - 동기화, B - 110 번
- 나. A - 동기화, B - 143 번
- 다. A - 비동기화, B - 143 번
- 라. A - 비동기화, B - 110 번

**정답:** 라

**해설**
POP3는 **비동기화** 방식이며 기본 포트는 **110**이다. 143은 IMAP 포트이다.

---

---

# 4강

---

## 1. (기출 14회) — PE 파일 형식

**문제**
파일 포맷인 PE(Portable Executable) 파일에 대한 설명으로 **옳지 않은** 것은?

**보기**
- 가. 리눅스에서 사용되는 실행 파일 형식이다.
- 나. 섹션 헤더에는 각 섹션에 대한 파일/메모리에서의 크기, 위치, 속성 등이 포함된다.
- 다. 파일의 내용은 보통 코드(.text), 데이터(.data), 리소스(.rsrc) 섹션에 나뉘어서 저장된다.
- 라. SYS 파일은 PE 파일이다.

**정답:** 가

**해설**
PE는 **Windows** 실행 파일 형식이다. 리눅스는 **ELF**를 사용한다. 나·다·라(섹션 헤더, .text/.data/.rsrc, SYS=PE)는 타당.

---

## 2. (기출 20회) — PE 확장자

**문제**
Windows 운영체제에서 정의한 파일 포맷인 PE(Portable Executable) 형식의 확장자에 **해당하지 않는** 것은?

**보기**
- 가. COM
- 나. OCX
- 다. EXE
- 라. DLL

**정답:** 가

**해설**
PE는 EXE·DLL·OCX 등에 쓰인다. **COM**은 MS-DOS/초기 Windows의 단순 실행 형식으로 PE와 구조가 다르다.

---

## 3. (기출 19회) — 레지스트리

**문제**
윈도우(Windows) 기반 시스템은 사용자, 응용 프로그램 및 하드웨어 장치에 적합하도록 시스템을 구성하기 위해 필요한 정보를 저장하는 중앙 계층형 데이터베이스를 가지고 있다. 이것의 명칭은 무엇인가?

**보기**
- 가. FAT32
- 나. Kernel
- 다. Registry
- 라. NTFS

**정답:** 다

**해설**
해당 설명은 **Registry(레지스트리)** 이다. FAT32·NTFS는 파일시스템, Kernel은 OS 핵심부이다.

---

## 4. (기출) — 한글 2007 Recent File 레지스트리

**문제**
다음 그림은 레지스트리 `HKEY_CURRENT_USER\Software\HNC\Hwp\7.0\HwpFrame\Recent File`의 일부이다. 다음 중 그림에 대한 설명으로 **옳지 않은** 것은?

(Count=`0x0000000a (10)`, file0~file3 등 REG_BINARY 값이 표시됨.)

**보기**
- 가. 한글 2007 문서 편집기가 사용한 최근 문서의 파일 이름을 알려준다.
- 나. Count 값이 0x0000000a이므로 0~10번까지 목록을 보여준다.
- 다. file0은 가장 최근에 사용한 문서의 정보를 나타낸다.
- 라. file0 데이터의 첫 바이트가 44이므로 사용문서는 D 드라이브에 있다.

**정답:** 나

**해설**
Count=10은 파일 **개수**이며 File0~File9(10개)이다. “0~10번까지”(11개)라는 해석은 잘못이다. 가·다·라(최근문서 MRU, File0=최신, 44='D')는 타당.

---

## 5. (기출 15회) — 한글 2010 RecentFile 레지스트리

**문제**
다음 Windows 레지스트리에 대한 설명 중 **옳지 않은** 것은?

*(경로 예: `HKEY_CURRENT_USER\Software\HNC\Hwp\8.0\HwpFrame_KOR\RecentFile`, Count=9)*

**보기**
- 가. 한글 2010 문서 편집기가 사용한 최근 문서 파일 이름을 나타내고 있다.
- 나. 가장 최근에 사용된 파일명은 File0 키에 저장되어 있다.
- 다. Count가 9이므로 최근에 사용된 9개 파일 목록이 여기에 저장되어 있다.
- 라. 한글 2010에서 사용하는 레지스트리 값은 사용자에 의해 변경될 수 없다.

**정답:** 라

**해설**
HKCU 아래 값은 해당 사용자(또는 관리자)가 **변경 가능**하다. “변경될 수 없다”는 옳지 않다.

---

## 6. (기출 16회) — 한글 2020 실행흔적

**문제**
한글 2020 실행흔적 조사에 대한 설명 중 가장 적합하지 않은 것은?

**보기**
- 가. `C:\Windows\Prefetch`에서 프리페치 파일 확인
- 나. `C:\Users\user\AppData\Roaming\HNC\Office\Recent`에서 실행파일의 바로가기 확인
- 다. `C:\Users\user\AppData\Local\Temp`에서 임시 파일 확인
- 라. `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Run`에서 실행 파일 확인

**정답:** 라

**해설**
Run 키는 **부팅 시 자동실행** 프로그램용이다. 한글 2020 일반 실행/사용 흔적 조사와는 직접 관련이 적다. Prefetch·Recent·Temp는 적합.

---

---

# 5강

---

## 1. (기출 16회) — 안티포렌식 기법 연결

**문제**
안티-포렌식 기법과 그 대표적인 방법을 연결하였다. **옳지 않은** 항목은?

**보기**
- 가. 데이터 은닉 (Hiding) - 스테가노그래피(Steganography)
- 나. 데이터 암호화 (Encryption) - 역공학(Reverse Engineering)
- 다. 데이터 파괴 (Destruction) - 디가우저(Degausser)
- 라. 분석 시간 증가 (Reducing analyzability) - 코드 난독화(Code Obfuscation)

**정답:** 나

**해설**
데이터 암호화는 암호 알고리즘으로 보호하는 기법이며, **역공학**은 구조·동작 원리 분석으로 암호화의 대표 방법이 아니다. 가·다·라의 연결은 타당.

---

## 2. (기출 19회) — 스테가노그래피

**문제**
데이터 은닉기법 중 하나로 전달하려는 기밀정보를 JPEG, MP3 등의 멀티미디어 파일이나, HWP, DOC 등의 문서 파일에 인간의 인지능력으로는 원본과 구별 불가능하도록 숨기는 방법을 무엇이라고 하는가?

**보기**
- 가. 데이터 조작(Manipulation)
- 나. 슬래커(Slacker)
- 다. 데이터 암호화(Encryption)
- 라. 스테가노그래피(Steganography)

**정답:** 라

**해설**
원본과 구분 어렵게 파일에 정보를 **숨기는** 기법이 **스테가노그래피**이다.

---

## 3. (기출 17회·19회·20회) — 지식재산 보호 기술

**문제**
다음 보기는 지식재산을 보호하기 위한 기술을 설명하고 있다. 설명에 맞는 용어로 구성된 것을 고르시오.

> a. 원본이 훼손되지 않는 범위 내에서 사용자가 인식하지 못하도록 저작권 정보를 삽입하는 기술
> b. 전달하려는 정보를 이미지나 멀티미디어 파일 등에 암호화 하여 숨기는 기술
> c. 디지털 콘텐츠를 구매할 때 구매자의 정보를 삽입해 불법 배포 시 최초 배포자를 추적할 수 있는 기술

**보기**
- 가. a - Digital Watermark / b - Steganography / c - Fingerprinting
- 나. a - Steganography / b - Fingerprinting / c - Digital Watermark
- 다. a - Digital Watermark / b - Fingerprinting / c - Steganography
- 라. a - Fingerprinting / b - Steganography / c - Digital Watermark

**정답:** 가

**해설**
a=디지털 워터마크, b=스테가노그래피, c=핑거프린팅.

---

## 4. (기출 20회) — 안티포렌식 응용프로그램

**문제**
안티 포렌식 응용프로그램에 대한 설명으로 올바르지 **않은** 것은 무엇인가?

**보기**
- 가. 개인 PC 혹은 공공장소에서 사용하는 PC에 불필요하게 저장되거나 기록되어 의도하지 않게 정보가 유출되는 피해를 방지하기 위한 프로그램이다.
- 나. 실시간 파일 암호화를 지원하는 트루크립트는 파일 내부나 디스크 파티션에 가상의 암호화된 디스크를 생성할 수 있다.
- 다. 마이크로소프트사에서 만든 비트 라커는 자신의 시스템에 인증받지 않은 디스크 변경을 보호할 수 있다.
- 라. 스테가노그래피는 사진, 동영상 등의 정보를 암호화해서 보호할 수 있는 프로그램이다.

**정답:** 라

**해설**
스테가노그래피는 **암호화가 아니라** 다른 매체에 **숨기는** 기술이다. “암호화해서 보호”라는 설명은 옳지 않다.

---

## 5. (기출 5회·8회) — 안티포렌식 응용프로그램(DRM)

**문제**
다음 중 안티 포렌식 응용프로그램 설명 중 **옳지 않은** 것은?

**보기**
- 가. 비트로커(BitLocker): 마이크로소프트사의 Windows용 상용 암호화 프로그램으로 사용이 간편하고 전체 드라이브를 암호화할 수 있으며 자신의 시스템에 인증받지 않은 변경에 대해 보호하는 기술이다.
- 나. 트루크립트(TrueCrypt): 실시간 파일 암호화를 지원하는 응용 소프트웨어로 무료 배포되고 소스코드가 공개되어 있으며 파일 내부나 디스크 파티션에 가상의 암호화 디스크를 생성하거나 전체 하드디스크를 암호화할 수 있다.
- 다. 스테가노그래피(steganography): 전달하려는 기밀정보를 그래픽, 사진, 영화, 소리 파일 등에 암호화해 숨기는 응용 암호 기술로써, 정보 교환하고 있다는 것을 숨기면서 통신하는 기술이다.
- 라. DRM(Digital Right Management): 문서 보안에 초점을 맞춘 기술로 문서의 열람/편집/인쇄에 대한 통제방법으로 접근 권한 설정은 제공하지 않는 기술이다.

**정답:** 라

**해설**
DRM은 열람·편집·인쇄 통제와 함께 **접근 권한 설정**을 포함한다. “접근 권한 설정을 제공하지 않는다”는 옳지 않다.

---

---

# 6강

---

## 1. (기출 17회) — 하드디스크 이미징 도구

**문제**
검찰 수사관은 압수한 컴퓨터의 하드디스크를 이미징하여 사본을 확보하려 한다. 이때 필요한 도구에 **해당하지 않는** 것은?

**보기**
- 가. Tableau
- 나. Winhex
- 다. FTK
- 라. EnCase

**정답:** 나

**해설**
Tableau(쓰기방지), FTK·EnCase는 이미징에 적합하다. **WinHex**는 주로 헥스 편집·분석용으로, 이미징 도구로는 덜 적합하다.

---

## 2. (기출 15회) — EWF(EnCase 이미지)

**문제**
다음은 무엇에 대한 설명인가?

> EnCase의 대표적인 디스크 이미지 파일 구조로써, 메타정보(케이스명, 조사관명, 시스템 정보 등)가 이미지 파일의 Header 영역에 저장되고 그 다음으로 Header에 대한 CRC 값이 저장되는 구조를 갖고 있다. Header 영역 이후로는 원본 저장매체의 섹터를 묶은 데이터 블록과 해당 데이터 블록에 대한 CRC 값이 저장된다. 데이터 블록의 기본 크기는 64섹터이다. 이런 구조의 데이터가 반복적으로 이미지 파일에 저장되고, 파일의 마지막 부분에는 각 데이터 블록의 md5값이 추가적으로 저장된다. 특정 블록의 데이터가 변경될 경우, 해당 블록의 CRC 값과 파일 끝의 md5 값으로 변경된 위치를 식별할 수 있다.

**보기**
- 가. SMART
- 나. Gfzip (Generic Forensic Zip)
- 다. EWF (Expert Witness Format)
- 라. AFF (Advanced Forensic Format)

**정답:** 다

**해설**
설명은 EnCase의 **EWF** 구조(헤더·CRC·64섹터 블록·말미 MD5)이다.

---

## 3. (기출 20회) — AD1 확장자

**문제**
디지털 포렌식 이미지 파일 중 "AD1" 확장자를 가진 파일은 어떤 포렌식 프로그램의 이미지 파일인가?

**보기**
- 가. FTK
- 나. EnCase
- 다. X-ways Forensic
- 라. PTK

**정답:** 가

**해설**
**AD1**은 AccessData **FTK**(Imager)의 이미지 형식이다. EnCase는 E01/EWF, X-Ways는 다양한 형식 지원.

---

---

# 7강

---

## 1. (기출 16회) — USB 사용 이력 레지스트리

**문제**
다음 중 USB 외부 저장장치의 사용 이력을 알아보기에 가장 **부적절한** 것은?

**보기**
- 가. HKEY_LOCAL_MACHINE
- 나. HKEY_CURRENT_USER
- 다. HKEY_USERS
- 라. HKEY_CURRENT_CONFIG

**정답:** 라

**해설**
USB 연결 이력은 주로 `HKLM\...\Enum\USBSTOR` 등에서 확인한다. HKCU·HKU도 사용자 설정과 관련될 수 있다. **HKEY_CURRENT_CONFIG**는 현재 하드웨어 프로필 정보로 사용 이력 분석에 부적절하다.

---

## 2. (기출 16회·20회) — WMIC diskdrive

**문제**
윈도우 10 운영체제에서 저장매체의 정보를 확인하기 위해 WMIC을 이용하여 다음 명령을 입력하였다. **확인할 수 없는** 것은?

```
C:\>wmic diskdrive get name,serialnumber,partitions,mediatype,size,systemname
```

**보기**
- 가. 저장장치의 용량과 시리얼 넘버
- 나. 저장장치의 시리얼 넘버와 컴퓨터 이름
- 다. 저장장치의 파티션 수와 시리얼 넘버
- 라. 저장장치의 모델명과 컴퓨터 이름

**정답:** 라

**해설**
주어진 속성은 name·serialnumber·partitions·mediatype·size·systemname이다. **모델명**은 `model` 속성으로 별도 조회해야 하므로 이 명령으로는 확인할 수 없다.

---

## 3. (기출 17회) — 포렌식 도구 법과학적 요구사항

**문제**
다음 디지털포렌식 도구에 대한 요구사항(기준)에 대한 설명 중 법 과학적으로 **가장 타당한** 것은?

**보기**
- 가. 입력되는 값이 다르더라도 출력되는 데이터는 항상 동일하게 산출되어야 한다.
- 나. 유사한 유형의 디지털 포렌식 도구를 이용하더라도 해당 분석도구의 결과를 검증할 수 있어야 한다.
- 다. 디스크 이미징 또는 특정 파일을 선별할 수 있는 기능을 갖춰야 한다.
- 라. 분석 과정에서 어떠한 결과가 발생할지 알 수 없으므로 모든 단계에서 목적과 예상 결과가 정의되지 않아도 문제 없다.

**정답:** 나

**해설**
**결과 검증 가능성**(유사 도구로 재현·일관성 확인)이 법과학적으로 타당하다. 가(다른 입력→동일 출력)는 오류, 다는 모든 도구의 필수 기능이 아님, 라는 목적·예상결과 정의가 필요하다고 부정.

---

---

# 8강

---

## 1. (기출 21회) — 파일 속성 시간 정보와 문서 위변조

**문제**
디지털 포렌식 기술로 문서의 위변조 여부를 판별할 수 있는 핵심정보로 파일 속성 시간 정보를 사용한다. 다음 중에서 파일 속성 시간 정보에 대한 설명으로 **올바르지 않은** 것은 무엇인가?

**보기**
- 가. 시간 정보를 저장한 파일 자체 정보만으로 위변조 여부를 판단하기 어렵지 않다.
- 나. 시간 속성 정보들 중에서 만든 날짜는 파일이 생성된 시간뿐만 아니라 파일이 수정된 시간까지 포함한다.
- 다. 시간 속성 정보들 중에서 수정한 날짜는 파일이 변경된 시간을 나타낸다.
- 라. 시간 속성 정보들 중에서 액세스한 날짜는 파일에 접근한 시간을 나타낸다.

**정답:** 가

**해설**
파일의 시간 정보는 변경이 가능하므로, 파일 자체 시간 정보만으로 위변조를 판단하기 **어렵다**. 다른 메타데이터·로그와 함께 분석해야 한다.

---

## 2. (기출 16회·17회·19회) — 스마트폰 킬 스위치

**문제**
다음은 스마트폰의 보안 기능에 관한 설명이다. 알맞은 것을 고르시오.

**[보기]**
이전에는 사용자가 스마트폰을 도난 또는 분실하였을 때 원격으로 통신사 또는 제조사에서 제공하는 원격 앱 서비스 기능을 사용하여 개인정보의 보호 및 타인의 재사용을 방지하였으나, 디바이스를 초기화 할 경우 원격 앱 기능까지 삭제되는 한계가 있었다. 이를 해결하기 위해 제조단계에서부터 도난방지 프로그램을 펌웨어 또는 운영체제에 탑재한 기능으로 디바이스가 초기화되더라도 분실 시 제3자가 재사용을 하지 못하도록 ( ) 기능이 도입되었다.

**보기**
- 가. 지문 인식
- 나. 패턴 보안
- 다. 핀 번호
- 라. 킬 스위치 (Kill Switch)

**정답:** 라

**해설**
**킬 스위치**는 초기화 후에도 재사용을 막는 도난방지 기능이다. 지문·패턴·핀은 잠금 인증 수단으로 본 설명과는 직접 관련 없다.

---

## 3. (기출 16회) — 이메일 보안 기술 (DKIM)

**문제**
다음 보기는 이메일 보안 기술 중 하나에 관한 설명이다. 무엇에 관한 설명인가?

**[보기]**
발신 메일의 헤더에 암호화된 서명을 추가하는 기술로 서명된 메일을 받는 이메일 서버는 이 기능을 이용해 해당 헤더를 복호화하고 메일이 전송된 후 변경되지 않았는지 확인한다. 이것은 도메인에서 보낸 발신 메일의 위조를 방지할 수 있는 기능으로 모든 메일에 디지털 서명을 추가하여 수신 측에서 메일이 위조되지 않았으며 전송 중 변경되지 않았음을 확인할 수 있다.

**보기**
- 가. SPF (Sender Policy Framework)
- 나. DKIM (DomainKeys Identified Mail)
- 다. PGP (Pretty Good Privacy)
- 라. S/MIME (Secure Multipurpose Internet Mail Extension)

**정답:** 나

**해설**
헤더에 도메인 연계 디지털 서명을 추가하는 기술은 **DKIM**이다. SPF는 발신 IP·허용 서버 검증, PGP·S/MIME은 개인 단위 암호화·서명이다.

---

## 4. (기출 21회) — 전자우편 설명 중 옳지 않은 것

**문제**
전자우편(E-Mail)에 관한 설명으로 **옳지 않은** 것은?

**보기**
- 가. 아웃룩(Outlook) 같은 전자우편 프로그램을 사용할 경우, 이메일을 삭제하더라도 데이터의 복구가 가능하다.
- 나. 포렌식 관점에서는 사건 정황을 이해하고 범행증거 자료의 수집을 위한 중요한 자료로 활용될 수 있다.
- 다. 이메일 백업파일에는 프로그램의 버전과 설치 경로가 포함되어 있다.
- 라. Microsoft Outlook에서 개인 아카이브 파일인 ‘.pst’ 파일은 사용자 컴퓨터 시스템에 이메일을 저장하는 데이터이다.

**정답:** 다

**해설**
이메일 백업파일에는 메시지·첨부·폴더 구조 등이 포함되나, **클라이언트 프로그램 버전·설치 경로**는 일반적으로 포함되지 않는다(시스템 파일·레지스트리 등에 별도 저장).

---

## 5. (기출 21회) — 스팸 필터링 도구

**문제**
다음에서 설명하고 있는 스팸 필터링 도구는 무엇인가?

**[보기]**
이것은 룰(Rule) 기반으로 메일 헤더와 내용 분석을 실시간으로 차단 리스트를 참고해 스팸 메일 여부를 판단하는 도구이다. 이것은 신뢰와 학습효과를 바탕으로 한다.

**보기**
- 가. 액세스 DB 활용기법
- 나. 스팸 차단 리스트 기법
- 다. 스팸 어쌔신 (SpamAssassin)
- 라. 인플렉스 (Inflex)

**정답:** 다

**해설**
룰·차단리스트·학습을 결합한 도구로 **SpamAssassin(스팸 어쌔신)**이 해당한다.

---

## 6. (기출 19회) — 웹서버 접속 로그

**문제**
웹서버 접속 로그(Access Log, Error Log)와 관련된 설명 중 **틀린** 것은?

**보기**
- 가. 아파치의 경우 리눅스 계열에서 설치 방법(RPM 설치, 컴파일 설치)에 따라 Default 로그 저장 경로가 다르다.
- 나. 아파치의 경우 리눅스의 종류에 따라 Default 로그 저장 경로가 다른 경우가 있다.
- 다. IIS의 경우 `%System Drive%\inetpub\logs\LogFiles`가 Default 로그 저장 경로이다.
- 라. 접속로그는 사용자로부터 수신한 HTTP의 헤더 정보 뿐만 아니라 사용자 Request 패킷(payload) 내용까지 포함한다.

**정답:** 라

**해설**
접속 로그는 URL·메서드·상태코드·UA 등 헤더·요청 메타정보를 기록하며, **Request payload 내용까지 포함하지 않는 것이 일반적**이다.

---

---

# 9강

---

## 1. (기출 15회) — OSI 7계층 응용계층 프로토콜

**문제**
다음 OSI 7 Layer 중 7계층인 응용계층(Application Layer)와 관련된 프로토콜에 대한 설명 중 **옳지 않은** 것은?

**보기**
- 가. SMB(Server Message Block) 프로토콜은 컴퓨터의 응용 프로그램에서 파일을 읽고 쓸 수 있으며 컴퓨터 네트워크상의 서버 프로그램에서 서비스를 요청할 수 있도록 지원하는 네트워크 파일 공유 프로토콜이다.
- 나. HTTP(HyperText Transfer Protocol)는 인터넷 상에서 데이터(HTML)를 주고 받기 위한 서버/클라이언트 기반의 프로토콜로써 일반적으로 TCP 80 Port를 사용하며, 암호화된 HTTPS의 경우 TCP 443 Port를 사용한다.
- 다. FTP(File Transfer Protocol)은 서버/클라이언트 구조의 파일 전송 프로토콜로써 Passive 방식과 Active 방식을 제공한다. Passive 방식의 FTP는 일반적으로 Command Port로 TCP 21 Port를 사용하며, Data Port로 TCP 21 Port를 사용한다.
- 라. DNS(Domain Name System)는 호스트의 도메인명을 네트워크 주소인 IP 주소로 변환해 주는 프로토콜로써, 일반적으로 TCP 53 Port는 Zone-Transfer로 사용되며 UDP 53 Port는 DNS 표준 질의에 사용된다.

**정답:** 다

**해설**
Passive FTP의 Data Port는 TCP 21이 아니라 **동적 할당 포트**를 사용한다. Active 방식에서는 서버가 보통 TCP 20을 Data Port로 사용한다.

---

## 2. (기출 14회) — Encapsulation(캡슐화)

**문제**
네트워크 프로토콜에서 각 계층은 상위 계층으로부터 받은 모든 정보를 데이터로 보고 자신의 헤더를 앞에 추가하여 하위 계층으로 전달한다. 이러한 과정을 설명하는 용어는 무엇인가?

**보기**
- 가. Modulation
- 나. Encryption
- 다. Encoding
- 라. Encapsulation

**정답:** 라

**해설**
계층이 내려갈 때마다 헤더를 추가하는 과정이 **Encapsulation(캡슐화)**이다.

---

## 3. (기출 19회) — Ethernet 프로토콜

**문제**
다음 중 Ethernet 프로토콜에 대한 설명으로 **옳은 것을 모두** 고른 것은?

**보기 (ㄱ~ㄹ)**
- ㄱ. 접근 제어 방식으로는 CSMA/CD를 채택하고 있다.
- ㄴ. OSI 7 Layer Model을 기준으로 할 때, IGRP와 같은 계층에 속한다.
- ㄷ. Ethernet 프로토콜 프레임에서 사용하는 주소는 MAC 주소이다.
- ㄹ. 전송하고자 하는 데이터의 크기가 43바이트일 경우 전체 Ethernet 프레임의 크기는 60바이트이다.

**선택지**
- 가. ㄱ, ㄷ
- 나. ㄱ, ㄹ
- 다. ㄴ, ㄷ
- 라. ㄴ, ㄹ

**정답:** 가

**해설**
ㄱ·ㄷ은 옳다. ㄴ: IGRP는 네트워크 계층(3계층) 라우팅 프로토콜이고 Ethernet은 데이터링크(2계층)이다. ㄹ: Ethernet 최소 프레임은 **64바이트**이므로 43바이트 데이터 시 패딩으로 64바이트가 되며 60바이트가 아니다.

---

## 4. (기출 15회) — IP·MAC·포트 중복 사용

**문제**
인터넷상의 호스트 간에 패킷의 송수신을 위해서는 IP 주소, MAC 주소, 포트(Port)번호가 필요하다. 아래는 세 요소의 중복 사용 여부에 대한 설명이다. **옳은 것은?** (여기서 IP는 공인 IP를 말한다)

**보기**
- 가. IP 주소, MAC 주소, 포트 번호를 각 호스트에서 중복 사용해도 패킷의 송수신이 가능하다.
- 나. 포트번호를 각 호스트에서 중복 사용해도 패킷의 송수신이 가능하다.
- 다. MAC 주소를 각 호스트에서 중복 사용해도 패킷의 송수신이 가능하다.
- 라. IP 주소를 각 호스트에서 중복 사용해도 패킷의 송수신이 가능하다.

**정답:** 나

**해설**
공인 IP·MAC은 중복 시 충돌로 통신이 불가하다. **포트 번호**는 호스트마다 동일하게(예: 80) 사용해도 문제 없다.

---

## 5. (기출 17회) — LAN 설명

**문제**
LAN(Local Area Network)에 대한 설명으로 **가장 거리가 먼** 것은?

**보기**
- 가. LAN은 비교적 가까운 거리에 위치한 장치들을 서로 연결한 네트워크이다.
- 나. LAN은 도시와 같은 넓은 지역, 국가나 대륙 같은 광범위한 지역에 걸쳐 구성된다.
- 다. LAN은 장치들을 연결하는 형식, 즉 토폴로지에 따라 링형, 버스형, 스타형 등으로 분류된다.
- 라. LAN의 전송률은 10Mbps에서 시작하여, 100Mbps, 1Gbps, 10Gbps 이상으로 바뀌어 가고 있다.

**정답:** 나

**해설**
나번은 도시 규모(MAN)·국가/대륙(WAN) 설명으로 LAN과 거리가 멀다.

---

## 6. (기출 14회) — 규모에 따른 네트워크 분류

**문제**
다음은 규모에 따른 네트워크를 분류한 설명이다. ⓐ, ⓑ, ⓒ에 들어갈 용어가 올바르게 짝지어진 것은?

- ⓐ 일반적으로 한 건물 안에서나 제한된 지역 내에서 컴퓨터 및 주변장치 등을 연결하여 정보와 프로그램을 공유할 수 있도록 해주는 네트워크
- ⓑ 최대 범위가 수십 km 정도이며 고속 전송 기능을 가진 네트워크로 도시나 큰 캠퍼스를 하나로 연결해 주는 네트워크
- ⓒ 도시와 국가, 대륙 등 지역적으로 넓은 범위에 걸쳐 구축하는 다양하고 포괄적인 컴퓨터 통신망

**보기**
- 가. LAN - MAN - WAN
- 나. WAN - MAN - LAN
- 다. LAN - WAN - MAN
- 라. MAN - WAN - LAN

**정답:** 가

**해설**
ⓐ LAN, ⓑ MAN, ⓒ WAN.

---

## 7. (기출 14회) — 망형 토폴로지 회선·포트 수

**문제**
노드 25개를 망형 토폴로지로 연결하려고 할 때 필요한 회선의 수와 노드당 필요한 포트의 개수는?

**보기**
- 가. 300(개), 12(개)
- 나. 300(개), 24(개)
- 다. 600(개), 12(개)
- 라. 600(개), 24(개)

**정답:** 나

**해설**
회선 수 = n(n−1)/2 = 25×24/2 = **300**. 노드당 포트 = n−1 = **24**.

---

---

# 10강

---

## 1. (기출 14회) — NAT

**문제**
외부 네트워크에 알려진 것과 다른 사설 IP주소를 사용하는 내부 네트워크에서, 외부 네트워크와의 호환성을 위하여 IP주소를 변환하여 매칭시켜주는 역할을 하는 것은 무엇인가?

**보기**
- 가. DNS
- 나. DDNS
- 다. DHCP
- 라. NAT

**정답:** 라

**해설**
사설 IP를 공인 IP로 변환·매칭하는 기술이 **NAT**이다.

---

## 2. (기출 15회) — NAT 작동·보안

**문제**
NAT(Network Address Translation)에 대한 설명이다. **옳은 항목을 모두** 고르시오.

- ㉠ NAT는 네트워크 계층과 전송 계층에서 작동한다.
- ㉡ 정적 NAT는 하나의 내부 IP주소와 외부 IP주소를 1:1로 매핑한다.
- ㉢ 동적 NAT는 여러 개의 내부 IP주소와 여러 개의 외부 IP주소를 n:m으로 매핑한다.
- ㉣ NAT는 내부 시스템의 네트워크 구조를 노출하지 않는다는 보안상 이점이 있다.

**보기**
- 가. ㉠, ㉡
- 나. ㉠, ㉡, ㉢
- 다. ㉡, ㉢, ㉣
- 라. ㉠, ㉡, ㉢, ㉣

**정답:** 라

**해설**
㉠~㉣ 모두 옳은 설명이다.

---

## 3. (기출 18회) — Windows IP 주소 조회 순서

**문제**
윈도우 10을 사용하는 사용자가 대한민국 홈페이지(https://www.korea.go.kr)에 접속하기를 원한다. IP 주소를 얻어오는 순서가 **옳은** 항목은?

**보기**
- 가. 캐시 → Hosts파일 → 로컬 DNS 서버
- 나. Hosts파일 → 캐시 → 로컬 DNS 서버
- 다. 로컬 DNS 서버 → 캐시 → Hosts파일
- 라. Hosts파일 → Hosts파일 → 캐시

**정답:** 가

**해설**
로컬 DNS 캐시 → Hosts 파일 → 로컬 DNS 서버 순이다.

---

## 4. (기출 21회) — 데이터 링크 계층

**문제**
OSI 7계층에서 프레임 데이터의 전송 과정 중 데이터 오류의 검출 및 회복과 흐름 제어를 조절하여 효율성을 향상시키는 계층에 해당하는 것은?

**보기**
- 가. 전송 계층
- 나. 물리 계층
- 다. 네트워크 계층
- 라. 데이터 링크 계층

**정답:** 라

**해설**
프레임 단위 오류 검출·회복·흐름 제어는 **데이터 링크 계층**의 역할이다.

---

## 5. (기출 14회) — ARP

**문제**
호스트나 라우터는 논리 주소(IP Address)를 가지고 있다. 그러나 IP 데이터그램은 물리적 네트워크를 통과하기 위해 프레임으로 캡슐화 되어야 한다. 이것은 송신자가 수신자의 물리적 주소(MAC Address)를 알 필요가 있음을 의미한다. 이때 논리적 주소를 물리적 주소로 바꾸어 주는 프로토콜은 무엇인가?

**보기**
- 가. RARP
- 나. ARP
- 다. ICMP
- 라. IGMP

**정답:** 나

**해설**
IP→MAC 변환은 **ARP**. (반대는 RARP)

---

## 6. (기출 20회·21회) — IP 패킷 TTL

**문제**
다음 중 IP 패킷 구조 옵션 내용 중 하나이다. 해당되는 옵션 내용에 대한 가장 올바른 옵션 명을 선택하시오.

> 라우팅(routing) 과정에서 라우터를 몇 개 이상 통과하면 해당 패킷을 버릴지를 알려준다. 라우터 하나를 지날 때마다 값이 1씩 줄어들고, 0이 되면 해당 패킷은 버려진다.

**보기**
- 가. IHL (Internet Header Length)
- 나. TOS (Type Of Service)
- 다. Flag
- 라. TTL (Time To Live)

**정답:** 라

**해설**
홉마다 1 감소·0이면 폐기하는 필드는 **TTL**이다.

---

## 7. (기출 18회) — TCP와 UDP 비교

**문제**
다음 중 TCP와 UDP에 대한 비교로 **옳지 않은** 것은?

**보기**
- 가. TCP와 UDP는 모두 오류 제어를 제공해 전달되는 패킷은 항상 발신지에서 수신지로 정확히 전달된다.
- 나. TCP는 연결지향형 서비스를 제공하고 UDP는 비연결형 서비스를 제공하므로 TCP가 UDP에 비해 신뢰성이 높다.
- 다. UDP는 TCP에 비해 오버헤드가 적다.
- 라. TCP는 연결 설정 및 연결 종료 과정이 있고 UDP는 연결 설정 및 연결 종료 과정이 없다.

**정답:** 가

**해설**
UDP는 비연결형으로 **오류 제어·재전송이 없다**. “모두 오류 제어로 항상 정확히 전달”은 틀리다.

---

## 8. (기출 21회) — TCP와 UDP 차이점

**문제**
TCP와 UDP의 차이점에 관한 설명으로 **옳지 않은** 것은?

**보기**
- 가. UDP가 TCP보다 신뢰성이 낮다.
- 나. TCP는 연결형, UDP는 비연결형 서비스를 제공한다.
- 다. TCP는 체크섬을 통한 오류검출을 선택적으로 할 수 있고, UDP는 필수적으로 한다.
- 라. UDP 헤더의 크기는 8바이트로 TCP 헤더에 비해 단순하다.

**정답:** 다

**해설**
TCP·UDP **모두** 체크섬 오류 검출을 한다. “TCP만 선택적·UDP만 필수”는 틀린 설명이다.

---

---

# 11강

---

## 1. (기출 20회) — TCP Three-way Handshake

**문제**
다음 중 TCP에 대한 설명으로 **옳지 않은** 것은?

**보기**
- 가. 종단간 전송된 데이터의 전송 여부를 보증한다.
- 나. TCP는 새로운 연결을 맺기 위해 SYN, ACK라고 하는 Flag를 사용한다.
- 다. TCP는 window라고 하는 흐름제어정보를 사용한다.
- 라. TCP 연결은 Two-way Handshake 기법을 이용한다.

**정답:** 라

**해설**
TCP는 **Three-way Handshake**를 사용한다(Two-way가 아님). SYN·ACK 플래그 사용·윈도우 흐름제어는 맞는 설명이다.

---

## 2. (기출 19회) — ICMP

**문제**
인터넷(IP) 계층에서 노드들 사이에 오류나 통신 제어를 위한 메시지를 보고할 목적으로 만들어진 프로토콜은 무엇인가?

**보기**
- 가. ARP
- 나. RARP
- 다. IGMP
- 라. ICMP

**정답:** 라

**해설**
오류·통신제어 메시지 보고는 **ICMP**. IGMP는 멀티캐스트 그룹 관리용이다.

---

## 3. (기출 19회) — 응용 계층에서 동작하지 않는 프로토콜

**문제**
TCP/IP 프로토콜 중 응용계층에서 동작하지 **않는** 것은?

**보기**
- 가. SMTP(Simple Mail Transfer Protocol)
- 나. SNMP(Simple Network Management Protocol)
- 다. IGMP(Internet Group Management Protocol)
- 라. FTP(File Transfer Protocol)

**정답:** 다

**해설**
**IGMP**는 네트워크 계층에서 동작한다. SMTP·SNMP·FTP는 응용 계층이다.

---

## 4. (기출 20회) — SNMP

**문제**
다음 중 SNMP에 관한 설명으로 **옳지 않은** 것은?

**보기**
- 가. SNMP는 관리자와 에이전트 사이에 교환되는 패킷의 형식을 정의한다.
- 나. SNMP에서 관리정보는 암호화되어 관리자 시스템에게 전송된다.
- 다. MIB는 관리 대상과 관련된 관리 정보의 집합이다.
- 라. SMI는 객체의 이름을 붙이고 객체 유형을 정의한다.

**정답:** 나

**해설**
SNMPv1·v2는 기본적으로 암호화하지 않는다. 버전 구분 없이 “암호화되어 전송된다”는 틀린 설명이다.

---

## 5. (기출 14회) — VPN 터널링 프로토콜 (3계층 아닌 것)

**문제**
가상 사설망(Virtual Private Network)의 터널링 프로토콜 중 3계층의 프로토콜이 **아닌** 것은?

**보기**
- 가. IPSec
- 나. ATMP
- 다. VTP
- 라. PPTP

**정답:** 나, 다

**해설**
정답이 2개라고 명시. **ATMP**는 잘못된 명칭(실제 ATM/L2TP 등). **VTP**는 VLAN 정보 교환용으로 2계층에서 동작. IPSec·PPTP는 3계층 관련 터널링으로 설명.

---

## 6. (기출 20회) — IPsec 특징

**문제**
VPN에서는 안전한 통신을 제공하기 위해 IPsec 프로토콜을 사용한다. 다음 중 IPsec 프로토콜의 특징으로 **옳지 않은** 것은?

**보기**
- 가. 전송 모드에서는 Payload만 인증·암호화를 수행한다.
- 나. 동적인 키 관리를 통해 특정 트래픽 유형을 확인하거나 전송을 차단할 수 있다.
- 다. 터널 모드에서는 IP Header만 인증·암호화를 수행한다.
- 라. 전송 계층보다 아래에 위치하며 사용자 시스템이나 서버 시스템의 소프트웨어를 바꿀 필요가 없다.

**정답:** 다

**해설**
터널 모드는 IP Header만 아니라 **전체 IP 패킷**을 인증·암호화한다.

---

---

# 12강

---

## 1. (기출 20회) — 무선 LAN Wi-Fi 규격

**문제**
다음 중 무선(Wireless) LAN Wi-Fi 규격(표준 위원회)에 해당하는 것은?

**보기**
- 가. IEEE 802.3
- 나. IEEE 802.11
- 다. IEEE 802.15.1
- 라. IEEE 802.16

**정답:** 나

**해설**
Wi-Fi는 **IEEE 802.11**. 802.3은 Ethernet, 802.15.1은 Bluetooth, 802.16은 WiMAX.

---

## 2. (기출 17회·18회·21회) — Bluetooth

**문제**
디지털 기기의 근거리 무선 통신을 할 수 있는 WPAN 기술로 2.4GHz의 비면허대역을 사용하면서 시간 슬롯마다 79개의 채널 중 한 채널로 임의 선택해 전송하는 채널 홉핑(Channel hopping) 방식을 사용하는 기술은 무엇인가?

**보기**
- 가. WiMAX
- 나. Bluetooth
- 다. LTE
- 라. Wi-Fi

**정답:** 나

**해설**
채널 홉핑·2.4GHz WPAN은 **Bluetooth** 특징이다.

---

## 3. (기출 14회) — WPA

**문제**
아래 설명 중 ( )에 들어갈 단어가 무엇인지 고르시오.

> 2002년 ( )은 802.11i 무선네트워크 보안 표준이 제정되기 전 무선 환경에서 사용하던 보안 인증 심사규격의 명칭이다. 이후 2004년 802.11i 제정 후 이와 유사한 내용의 보안 인증 심사규격인 ( )2를 발표하고, 2018년에는 기존 PSK(Pre-Shared Key) 인증 방법을 대체하는 SAE (Simultaneous Authentication of Equals)를 지원하는 ( )3를 발표하였다.

**보기**
- 가. WEP (Wired Equivalent Privacy)
- 나. TKIP (Temporal Key Integrity Protocol)
- 다. WPA (Wi-Fi Protected Access)
- 라. AES (Advanced Encryption Standard)

**정답:** 다

**해설**
괄호는 **WPA / WPA2 / WPA3** 계열이다.

---

## 4. (기출 21회) — Teardrop 공격

**문제**
아래에서 설명하고 있는 네트워크 공격 방법은 무엇인가?

> 헤더가 조작된 일련의 IP 패킷 조각들을 전송하는 네트워크 공격 기법이다. 이를 통해서 수신자 컴퓨터는 완성되지 않은 패킷 조각들을 무한히 기다리거나 처리 완료하지 못해서 네트워크가 무한히 지연되기도 한다.

**보기**
- 가. Smurf 공격
- 나. UDP Flooding 공격
- 다. Teardrop 공격
- 라. TCP SYN Flooding

**정답:** 다

**해설**
조작된 IP 조각·재조립 장애는 **Teardrop**이다.

---

## 5. (기출 18회) — TCP Teardrop Attack

**문제**
아래 설명에 해당하는 공격 기법은?

> 공격자가 TCP/IP 통신에서 보내는 쪽에서는 IP 데이터그램을 쪼개고 받는 쪽에서 이를 합치는 과정을 인위적으로 과도하게 부하를 발생시켜서 대상 장비를 서비스 불능상태로 만드는 DoS 공격

**보기**
- 가. Land Attack
- 나. TCP Checksum Error Attack
- 다. TCP Teardrop Attack
- 라. UDP Flooding

**정답:** 다

**해설**
쪼개고 합치는 과도한 부하 → **TCP Teardrop Attack**.

---

## 6. (기출 19회) — DNS Spoofing

**문제**
특정 시스템의 DNS 요청에 대해 잘못된 IP로 응답하는 패킷을 전송하여 해당 시스템이 패킷을 정상적으로 보내지 못하도록 하는 공격은 무엇인가?

**보기**
- 가. DNS Amplification 공격
- 나. IP Flooding 공격
- 다. ARP Spoofing 공격
- 라. DNS Spoofing 공격

**정답:** 라

**해설**
잘못된 DNS 응답 IP → **DNS Spoofing**.

---

## 7. (기출 19회) — MITM

**문제**
네트워크상에 존재하는 컴퓨터와 컴퓨터 사이의 활성화 상태인 세션을 가로채기하여 데이터를 위조 또는 변조하는 공격 방법은 무엇인가?

**보기**
- 가. MITM
- 나. DoS
- 다. DDoS
- 라. LAND

**정답:** 가

**해설**
세션 가로채기·데이터 위변조는 **MITM(중간자 공격)**이다.

---

---

# 13강

---

## 1. (기출 14회·21회) — IDS False Positive

**문제**
침입탐지시스템(IDS)에서 False Positive가 의미하는 것은 무엇인가?

**보기**
- 가. 공격이 분명한데 탐지하지 못하는 것
- 나. 공격이 아닌데 공격으로 인식하는 것
- 다. 공격에 대해 로그를 남기는 것
- 라. 특정 공격을 다른 공격으로 인식하는 것

**정답:** 나

**해설**
False Positive는 **잡는다(긍정)**인데 틀린 경우 → 공격이 아닌데 공격으로 인식. False Negative는 공격인데 탐지 못함(가).

---

## 2. (기출 20회) — IDS·IPS 특징이 아닌 것

**문제**
다음 중 침입탐지시스템(IDS)와 침입방지시스템(IPS)의 특징이 아닌 것은?

**보기**
- 가. 침입탐지시스템(IDS)은 사이버위협 이벤트를 탐지만 한다.
- 나. 침입방지시스템(IPS)은 사이버위협 이벤트를 탐지 및 차단을 다 한다.
- 다. 침입방지시스템(IPS)의 일반적인 위치는 침입차단시스템(F/W) 다음에 설치한다.
- 라. 침입탐지시스템(IDS)은 제로데이(Zero-day) 공격을 탐지할 수 있다.

**정답:** 라

**해설**
가·나·다는 일반적인 특징. 룰/시그니처 기반 IDS는 **알려진(원데이) 공격**에 강하고, 제로데이를 잘 탐지한다고 보기 어렵다 → 라가 특징이 아님.

---

## 3. (기출 17회) — Firewall 설명으로 가장 먼 것

**문제**
침입차단시스템(Firewall)에 대한 설명으로 가장 거리가 먼 것은?

**보기**
- 가. 침입차단시스템은 외부 망에서 내부 망으로 접근하는 주요 입구에 설치되어야 한다.
- 나. ACL(Access Control List)을 기반으로 패킷 헤더를 검색해 패킷을 선택적으로 허용 또는 거부한다.
- 다. 침입차단시스템은 내·외부로부터의 불법적인 접근이나 공격을 차단한다.
- 라. 침입차단시스템은 NAT(Network Address Translation) 기능을 이용하여 내부 사용자의 IP가 외부에 공개되지 않도록 할 수 있다.

**정답:** 다

**해설**
방화벽은 주로 **외부→내부** 불법 접근 차단. 내부 발 공격·불법 접근까지 모두 차단한다고 보기는 어렵고, 내부 대응은 NAC·백신 등 다른 솔루션이 담당.

---

## 4. (기출 18회) — 방화벽 방식·프로토콜(계층) 짝

**문제**
방화벽(Firewall) 보안 시스템의 적용 방식과 관련 프로토콜을 짝지은 것이다. 옳은 항목은?

**보기**
- 가. 패킷 필터링(Packet Filtering) - 응용 계층
- 나. 응용 프록시(Application Proxy) - 네트워크 계층
- 다. 상태 정밀 검사 방식(Stateful Packet Inspection) - 데이터링크 계층
- 라. 회로 레벨 게이트웨이(Circuit Level Gateway) - 세션 계층

**정답:** 라

**해설**
패킷 필터링은 **네트워크(3)**, 응용 프록시는 **응용(7)**, 상태 정밀 검사는 **네트워크(3)·전송(4)**. 회로 레벨 게이트웨이는 **세션 계층**에서 동작 → 라만 옳음.

---

## 5. (기출 15회·18회) — 방화벽 특성(서비스 제어)

**문제**
방화벽(Firewall)의 특성 중 다음 설명이 의미하는 것은?

> 안에서 밖으로 혹은 밖에서 안으로 접근할 수 있는 인터넷 서비스 유형을 결정함. 침입차단시스템은 IP 주소와 TCP 포트 번호를 근거로 트래픽을 필터링하기도 하고, 프록시 소프트웨어를 이용하여 각 서비스 요청을 전달하기에 앞서 수신하고 해석할 수 있음.

**보기**
- 가. 서비스 제어
- 나. 사용자 제어
- 다. 행동 제어
- 라. 목적지 제어

**정답:** 가

**해설**
인터넷 **서비스 유형** 결정·IP/포트·프록시로 서비스 요청 처리 → **서비스 제어**.

---

## 6. (기출 15회) — 침입차단시스템 기술

**문제**
다음 중 설명하고 있는 침입차단시스템 기술은?

> 응용 지원 게이트웨이에 비해 처리속도가 빠르고 비용/오버헤드 효율이 높다는 점과 사용자에게 투명성을 제공한다는 장점이 있으나, 패킷 내의 데이터 부분에 포함된 유해 정보나 바이러스에 감염된 메일 전송 시 차단 불가능하다는 점, 그리고 필터링의 규칙의 개수 및 순서에 따라 부하가 가중된다는 단점이 있다.

**보기**
- 가. 패킷 필터링 게이트웨이
- 나. 회로 레벨 게이트웨이
- 다. 응용 게이트웨이
- 라. 상태 정보 검사 방식

**정답:** 가

**해설**
빠른 처리·투명성·콘텐츠(바디) 미검사·규칙 개수/순서에 따른 부하 → **패킷 필터링 게이트웨이**.

---

# 14강

---

## 1. (기출 16회·18회) — OSI 계층과 DoS 방법

**문제**
OSI 7 계층(Layers)과 각 계층을 통한 서비스 거부(Denial of Service) 방법이 올바르지 않은 것은?

**보기**
- 가. 3계층 - UDP Flooding
- 나. 4계층 - SYN Flooding
- 다. 5계층 - Malformed SSL Request
- 라. 7계층 - slowloris

**정답:** 가

**해설**
UDP Flooding은 **4계층(전송 계층)** 공격이다. 3계층으로 짝지은 가가 틀림. 나·다·라의 계층 매칭은 맞음.

---

## 2. (기출 18회) — ARP 설명으로 옳지 않은 것

**문제**
ARP(Address Resolution Protocol)에 대한 설명으로 옳지 않은 항목은?

**보기**
- 가. 목적지 IP(Internet Protocol) 주소를 목적지 MAC(Media Access Control) 주소로 변환해 주는 프로토콜이다.
- 나. ARP 캐시는 가장 최근에 매핑된 IP와 하드웨어 주소를 관리한다.
- 다. MAC 주소를 얻기 위하여 ARP reply 패킷을 브로드캐스팅한다.
- 라. ARP 스푸핑 공격의 원인이 된다.

**정답:** 다

**해설**
MAC을 얻기 위해 **ARP request**를 브로드캐스트하고, **ARP reply는 유니캐스트**로 응답한다. reply를 브로드캐스트한다는 다는 옳지 않음.

---

## 3. (기출 15회·16회) — ARP 동작 순서와 스푸핑 개입 단계

**문제**
다음은 ARP 프로토콜의 동작 과정이다. 올바른 순서와 공격자가 ARP 스푸핑을 위해 개입하는 단계는 어디인가?

> A. 송신 호스트는 수신 호스트의 MAC 주소를 ARP 캐시에 저장한다.
> B. 송신 호스트는 수신 호스트 IP 주소를 입력한다.
> C. 자신의 IP 주소를 확인한 수신 호스트가 자신의 MAC을 ARP 패킷으로 전송한다.
> D. 송신 호스트의 네트워크 계층에서 자신의 MAC 주소와 목적지 IP 주소로 ARP 패킷을 브로드캐스트 한다.

**보기**
- 가. A-B-D-C, A
- 나. B-D-C-A, C
- 다. D-C-A-B, C
- 라. B-D-C-A, A

**정답:** 나

**해설**
순서: **B→D→C→A**. 스푸핑은 응답(리플라이) 단계 **C**에서 개입한다.

---

## 4. (기출 15회·19회) — 스위치 환경 스니핑 방법

**문제**
스위치 환경(Switch Network)에서 스니핑(Sniffing)을 하는 방법으로 옳지 않은 것은?

**보기**
- 가. 포트 미러링(Port Mirroring)이 가능한 스위치라면 연결된 포트에 미러링 설정을 한다.
- 나. ARP 캐시 포이즈닝(ARP Cache Poisoning)을 이용하여, 특정 패킷(packet)을 전송하여 경로를 조작한다.
- 다. 네트워크 탭(tap) 혹은 허브(Hub)를 설치하여, 스니핑을 진행한다.
- 라. 대상 PC에 다량의 ICMP 패킷을 보내서 패킷을 전달받는다.

**정답:** 라

**해설**
가·나·다는 스위치 환경에서 스니핑에 쓰이는 방법. 다량 ICMP는 스니핑 방법이 아님 → **라**.

---

## 5. (기출 18회) — NetFlow로 확인할 수 없는 정보

**문제**
NetFlow만을 통해 확인할 수 없는 정보를 고르시오.

**보기**
- 가. 출발지 주소/포트(IP source address/port)
- 나. 도착지 주소/포트(IP destination address/port)
- 다. 서비스 등급(Class of Service)
- 라. HTTP 내용(HTTP Body)

**정답:** 라

**해설**
NetFlow는 플로우 메타데이터(주소·포트·CoS 등)를 수집하고 **HTTP Body(페이로드)는 수집하지 않는다**.

---

## 6. (기출 20회) — 방화벽의 한계가 아닌 것

**문제**
다음 중 방화벽의 한계가 아닌 것은?

**보기**
- 가. 바이러스를 막을 수 없다.
- 나. 악의적인 내부 사용자의 공격을 막을 수 없다.
- 다. 방화벽을 통과하는 통신에 대한 제어가 불가능하다.
- 라. 새로운 형태의 공격을 막을 수 없다.

**정답:** 다

**해설**
방화벽은 통과 트래픽을 IP·포트·프로토콜 등으로 **제어할 수 있다**. 다가 “한계”가 아님. 가·나·라는 실제 한계.

---

## 7. (기출 21회) — 네트워크 패킷 캡처 최적 위치

**문제**
아래는 어떠한 네트워크 구조를 나타내고 있다. 용의자 甲과 乙 사이에 내부의 정보를 유출하고 있다는 정황이 있어 이를 확인하기 위해 네트워크 패킷을 캡처하고자 할 때, 네트워크 패킷을 캡처하기 위한 위치로 가장 적절한 것은?

> (구조) 방화벽 → 라우터 → IDS → 스위치 → (용의자 甲, 용의자 乙)

**보기**
- 가. 방화벽
- 나. 라우터
- 다. IDS
- 라. 스위치

**정답:** 라

**해설**
두 용의자가 같은 스위치에 연결되므로 **스위치**(포트 미러링 등)에서 캡처하는 것이 가장 적절하다.

---

# 15강

---

## 1. (기출 21회) — 스위치·라우터 설명

**문제**
다음은 네트워크 장비 중에서 스위치와 라우터에 대한 설명이다. 이 중에서 올바르지 않은 것은 무엇인가?

**보기**
- 가. 스위치에서 패킷 분석을 통해서 침입자 탐지가 가능하다.
- 나. 라우터에서 접근제어 리스트 정보에 RFC 정의 필터 및 보안 제어가 가능하다.
- 다. 라우터에서 ARP 정보에 IP주소 및 호스트 맵핑이 가능하다.
- 라. 스위치에서 Port 번호를 통해서 네트워크 구성 정보를 파악할 수 있다.

**정답:** 가

**해설**
스위치는 주로 데이터링크 계층에서 동작하며 **패킷 분석으로 침입자 탐지**하는 기능은 IDS 쪽이다. 가가 옳지 않음.

---

## 2. (기출 15회) — Ubuntu ACL 정책 순서

**문제**
우분투(Ubuntu) 방화벽의 접근제어목록(Access Control List, ACL) 기능을 이용하여 다음 목적에 적합한 ACL 정책을 수립하고자 한다. (가~라) 중 알맞은 것은? (단, ACL 정책의 Group 번호는 고려하지 않는다.)

> 1. 192.168.10.0/24 대역의 모든 패킷을 차단한다.
> 2. 1번 조건 중 예외로 192.168.10.1에서 들어오는 모든 패킷을 허용한다.
> 3. 192.168.10.0/24 대역 이외의 모든 대역에서 들어오는 패킷들은 허용한다.

**보기**
- 가.
 - access-list 10 deny 192.168.10.0 0.0.0.255
 - access-list 10 permit 192.168.10.1 0.0.0.0
 - access-list 10 permit ip
- 나.
 - access-list 10 permit 192.168.10.1 0.0.0.0
 - access-list 10 deny 192.168.10.0 0.0.0.255
 - access-list 10 permit ip
- 다.
 - access-list 10 deny 192.168.10.0 255.255.255.0
 - access-list 10 permit 192.168.10.1 255.255.255.255
 - access-list 10 permit ip
- 라.
 - access-list 10 permit 192.168.10.1 255.255.255.255
 - access-list 10 deny 192.168.10.0 255.255.255.0
 - access-list 10 permit ip

**정답:** 나

**해설**
예외 **permit(호스트)**이 대역 **deny**보다 위에 와야 하고, 와일드카드 마스크(`0.0.0.0`, `0.0.0.255`)를 쓰는 **나**가 맞다. 가는 deny가 먼저라 예외가 무효, 다·라는 서브넷 마스크 표기.

---

## 3. (기출 20회) — 방화벽 정책 Shadowing

**문제**
다음 중 Shadowing에 해당되는 정책은?

| 순번 | Src IP | Src Port | Dst IP | Dst Port | 프로토콜 | 행위 |
|---:|:---|:---|:---|:---|:---|:---|
| 1 | ANY | ANY | 192.168.10.100 | 80 | TCP | ACCEPT |
| 2 | 192.168.50.20 | ANY | 192.168.10.100 | 22 | TCP | ACCEPT |
| 3 | 192.168.50.30 | ANY | ANY | 22 | TCP | DENY |
| 4 | ANY | ANY | 192.168.10.100 | 443 | TCP | ACCEPT |
| 5 | ANY | ANY | ANY | ANY | ALL | DENY |

**보기**
- 가. 1번 정책
- 나. 2번 정책
- 다. 3번 정책
- 라. 4번 정책

**정답:** 다

**해설**

---

## 4. (기출 15회·19회) — HTTP 헤더 해석

**문제**
다음 보기는 HTTP 프로토콜 헤더를 표현한 것이다. 포렌식 분석가가 헤더에 대해 해석한 내용 중 가장 옳지 않은 것은?

> GET /KOR/Main.htm HTTP/1.1
> Accept: text/html, application/xhtml+xml, */*
> Referer: http://www.bestbestmall.com/KOR/main.htm
> Accept-Language: ko-KR
> User-Agent: Mozilla/5.0 (Windows NT 6.1; Trident/7.0; rv:11.0) like Gecko
> Accept-Encoding: gzip, deflate
> Host: www.bestbestmall.com
> Connection: Keep-Alive

**보기**
- 가. GET 메소드를 이용하여 특정 페이지(Main.htm)를 요청하였다.
- 나. 바로 이전에 접속한 페이지는 Referer를 통해 확인이 가능하다.
- 다. User-Agent를 통해 서버에 대한 다양한 정보를 알 수 있다.
- 라. 사용자가 방문하려는 도메인은 bestbestmall.com이다.

**정답:** 다

**해설**
User-Agent는 **클라이언트**(브라우저·OS) 정보이지 서버 정보가 아니다.

---

## 5. (기출 16회) — 웹 서버 로그 종류

**문제**
방문자가 웹 사이트에 접속하면 웹 서버에는 접속 관련 자료가 파일 형태로 기록된다. 다음과 같은 내용을 기록하는 파일은?

> 사용자가 사이트를 방문한 경로인 검색엔진의 종류, 사용된 키워드, 경유한 URL 경로 등

**보기**
- 가. 액세스 로그(Access Log) 파일
- 나. 에러 로그(Error Log) 파일
- 다. 리퍼러 로그(Referrer Log) 파일
- 라. 에이전트 로그(Agent Log) 파일

**정답:** 다

**해설**
검색엔진·키워드·경유 URL → **리퍼러(Referrer) 로그**.

---

## 6. (기출 20회) — RFC 3227 휘발성 순서

**문제**
RFC 3227에는 증거물 수집과 증거보관에 관한 지침이 있으며 휘발성 순서에 따라 먼저 수집해야 할 디지털 증거를 기술하고 있다. 다음 보기에서 휘발성이 빠른 순서대로 표기한 것을 고르시오.

> a. 라우팅 테이블, 메모리
> b. 임시 파일 시스템
> c. 레지스터, 캐시
> d. 디스크

**보기**
- 가. b, a, c, d
- 나. c, a, b, d
- 다. a, b, c, d
- 라. c, b, a, d

**정답:** 나

**해설**
휘발성 빠른 순: **c(레지스터·캐시) → a(라우팅 테이블·메모리) → b(임시 파일 시스템) → d(디스크)**.

---

## 7. (기출 20회) — 네트워크 증거 수집

**문제**
네트워크 증거수집에 대한 설명으로 올바르지 않은 것은 무엇인가?

**보기**
- 가. 네트워크 증거 수집 대상은 쉽게 변경되지 않으므로 수집하기 쉽다.
- 나. 먼저 수집해야 할 정보들로는 기본정보와 시간 정보, 사용자 정보를 구별할 수 있다.
- 다. 전원이 꺼지면 사라지기 쉬운 정보부터 빠르게 수집하는 것이 좋다.
- 라. 네트워크 증거는 무결성을 검증하기 어렵다.

**정답:** 가

**해설**
네트워크 증거는 **쉽게 변경**되고 수집·무결성 확보가 어렵다. 가의 “변경되지 않으므로 쉽다”가 옳지 않음.

---
