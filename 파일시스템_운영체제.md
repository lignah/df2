# 파일시스템과 운영체제

# 1강

---

## 1. (기출 18회)

**문제**
다음은 어떠한 파일을 Hex 에디터로 열어본 내용이다. 이 파일은 어떠한 확장자의 파일인가?

```
00000000h: 89 50 4E 47 0D 0A 1A 0A 00 00 00 0D 49 48 44 52
00000010h: 00 00 00 FA 00 00 00 FA 08 06 00 00 00 88 EC 5A
00000020h: 3D 00 00 00 01 73 52 47 42 00 AE CE 1C E9 00 00
00000030h: 00 04 67 41 4D 41 00 00 B1 8F 0B FC 61 05 00 00
00000040h: 00 09 70 48 59 73 00 00 0E C4 00 00 0E C4 01 95
```

**보기**
- 가. png
- 나. pdf
- 다. mp4
- 라. jpg

**정답:** 가

**해설**
이 값은 PNG 파일의 시그니처입니다. PNG 파일은 항상 `89 50 4E 47 0D 0A 1A 0A`로 시작합니다. 이 시그니처는 PNG 파일을 식별하는 데 사용됩니다.

---

# 2강

---

## 1. (기출 21회) — 플래시 메모리

**문제**
플래시 메모리는 하드디스크와 동일한 읽기, 쓰기 연산 이외에 소거 연산이 필요하다. 덮어쓰기(Overwrite)가 불가능한 플래시 메모리에서 이전에 기록한 데이터를 지우기 위한 소거 연산은 같은 위치에 데이터를 읽고 쓰는 연산에 비해 속도가 매우 느리다. 이러한 원인을 가장 적절하게 설명한 것을 고르시오.

**보기**
- 가. 블록단위로 인한 Freezing 현상
- 나. 페이지 단위로 인한 Freezing 현상
- 다. TRIM 명령으로 인한 Freezing 현상
- 라. Erase-Before-Write에 의한 Freezing 현상

**정답:** 라

**해설**
플래시 메모리는 쓰기 전에 반드시 기존 데이터를 지우는 소거 연산이 필요하며(Erase-Before-Write), 소거는 블록 단위로 수행되어 읽기·쓰기보다 매우 느리다. 가·나(블록/페이지 단위 자체)나 다(TRIM)로는 소거 연산이 느린 원인을 가장 적절히 설명하지 못한다.

---

## 2. (기출 21회) — 플래시 메모리

**문제**
다음 중 모바일 파일시스템에 사용되는 플래시 메모리에 대한 설명이 잘못된 것을 고르시오.

**보기**
- 가. 비휘발성 메모리로서 NOR 플래시 메모리와 NAND 플래시 메모리로 구분된다.
- 나. NOR 플래시 메모리는 Byte Addressing이 가능하지만 저장공간 크기를 쉽게 증가시킬 수 없다.
- 다. NAND 플래시 메모리는 페이지 단위로 밖에 접근할 수 없지만 저장공간을 NOR 플래시 메모리에 비해 쉽게 증가시킬 수 있다.
- 라. 현재 SSD, USB 메모리에 쓰이는 플래시 메모리 기반 저장장치는 모두 NOR 플래시 메모리를 사용한다.

**정답:** 라

**해설**
SSD·USB 등 대용량 저장장치는 **NAND** 플래시를 사용한다. NOR는 주로 빠른 읽기가 필요한 펌웨어·실행 코드 저장에 쓰인다. 가·나·다는 타당한 서술로 다룸.

---

## 3. (기출 18회) — 물리 디스크의 논리적 구조

**문제**
다음 중 가장 일반적으로 사용하는 유니코드 포맷으로 31비트의 유니코드를 1~6개 바이트에 나누어 저장하는 방식은 무엇인가?

**보기**
- 가. ①마스터 부트 레코드(MBR)-②볼륨 부트 레코드(VBR)-③파일시스템-④파티션
- 나. ①볼륨 부트 레코드(VBR)-②파티션-③마스터 부트 레코드(MBR)-④파일시스템
- 다. ①볼륨 부트 레코드(VBR)-②마스터 부트 레코드(MBR)-③파일시스템-④파티션
- 라. ①볼륨 부트 레코드(VBR)-②파일시스템-③파티션-④마스터 부트 레코드(MBR)

**정답:** 가

**해설**
디스크 논리 구조 관점으로 ① MBR(첫 섹터, 부트코드·파티션 테이블) → ② VBR(각 파티션 시작) → ③ 파일시스템 → ④ 파티션을 설명하며 정답을 가로 제시.

---

## 4. (기출 14회) — 하드디스크의 구조와 저장 방식

**문제**
디스크의 주소를 지정하는 방식에 대한 설명으로 옳지 않은 것은?

**보기**
- 가. CHS는 Cylinder, Head, Sector의 약어로 디스크의 물리적인 구조에 기반 주소 지정 방식이다.
- 나. CHS 방식은 초기 ATA 표준과 BIOS의 지원 비트의 차이로 인해 최대 504MB까지만 지정이 가능했다.
- 다. LBA 방식은 디스크 0번 실린더, 0번 헤드, 1번 섹터의 첫 번째(0번) 블록으로 지정하는 방식이다.
- 라. LBA 방식 초기에는 16bit로 처리하여 약 100GB가 최대 용량이다.

**정답:** 라

**해설**
초기 LBA는 **28비트**로 약 **137GB(128GiB)**까지 주소 지정이 가능했다. “16bit·약 100GB” 서술은 부정확. 가·나·다는 옳은 쪽으로 설명.

---

## 5. (기출 16회·18회) — MBR

**문제**
마스터 부트 레코드(MBR)는 저장매체의 첫 번째 섹터에 위치하며 부팅과 관련된 코드와 파티션 정보를 가지고 있다. 마스터 부트 레코드에는 최대 몇 개의 파티션 정보를 저장할 수 있는가?

**보기**
- 가. 3개
- 나. 4개
- 다. 5개
- 라. 제한 없음

**정답:** 나

**해설**
MBR 파티션 테이블은 64바이트이며 엔트리당 16바이트이므로 최대 **4개**. 확장 파티션으로 논리 파티션을 늘릴 수 있어도 MBR 자체 엔트리는 4개가 한도.

---

## 6. (기출 18회) — MBR

**문제**
용의자가 증거를 인멸하기 위해 사용 중인 하드디스크의 마스터 부트 레코드 정보를 삭제하였다. 포렌식 수사관인 여러분이 데이터 복구를 위해 마스터 부트 레코드 정보를 찾기 위해 검색해야 할 16진수 정보는 무엇인가?

**보기**
- 가. 0xAA55
- 나. 0xFFD8FFE0
- 다. 0xFFD8FFE1
- 라. 0xD0CF11E0A1B11AE1

**정답:** 가

**해설**
MBR 마지막 2바이트 시그니처가 **0xAA55**(엔디안에 따라 55 AA로 기록)이다. 나·다는 JPEG, 라는 MS Office OLE 시그니처로 MBR과 무관.

---

## 7. (기출 20회) — MBR

**문제**
다음 중 MBR(Master Boot Record)에 저장된 정보가 **아닌** 것은?

**보기**
- 가. 파일 할당 테이블 (File Allocation Table)
- 나. 파티션 테이블 엔트리 (Partition Table Entry)
- 다. 부트코드 (Boot Code)
- 라. 시그니처 (Signature)

**정답:** 가

**해설**
FAT는 파일시스템(파티션) 영역의 구조이지 MBR에 포함되지 않는다. MBR에는 부트코드·파티션 테이블 엔트리·시그니처가 있다.

---

## 8. (기출 20회) — MBR

**문제**
MBR(Master Boot Record)은 하나의 볼륨에 여러 파티션이 존재할 경우 필요한 영역으로 해당 파티션 테이블과 부팅이 가능한 부트 프로그램을 저장하고 있다. 다음 파티션 테이블의 정보에서 NTFS 영역에 해당하는 파티션 타입은 어떤 것인지 고르시오.

**보기**
- 가. 0x00
- 나. 0x07
- 다. 0x0C
- 라. 0x82

**정답:** 나

**해설**
- 0x00: 미사용/빈 파티션
- **0x07: NTFS(또는 exFAT)**
- 0x0C: FAT32(LBA)
- 0x82: Linux Swap

---

# 3강

---

## 1. (기출 16회) — 파일시스템의 구조

**문제**
파일 시스템의 논리적 구조 중 **아닌** 것은?

**보기**
- 가. 평면 디렉토리 구조
- 나. 다층 디렉토리 구조
- 다. 계층 디렉토리 구조
- 라. 그래프 디렉토리 구조

**정답:** 나

**해설**
‘다층 디렉토리 구조’는 일반적으로 말하는 파일시스템 논리 구조 용어가 아니다. 가(평면)·다(계층/트리)·라(그래프)는 표준적으로 다루는 구조.

---

## 2. (기출 19회) — 파일시스템의 구조

**문제**
다음 중 파일 시스템의 논리적 구조 중 루트(root) 디렉토리 밑에 여러 개의 하위 디렉토리를 갖는 구조는?

**보기**
- 가. 평면 디렉토리 구조
- 나. 계층형 디렉토리 구조
- 다. 2단계 디렉토리 구조
- 라. 그래프 디렉토리 구조

**정답:** 나

**해설**
계층형(트리) 구조는 루트 아래 여러 하위 디렉토리를 둘 수 있다. 평면은 하위 디렉토리 없음, 2단계는 사용자별 1디렉토리 수준, 그래프는 링크·다중 경로 가능 구조.

---

## 3. (기출 20회) — FAT32

**문제**
FAT32 파일 시스템에 관한 설명으로 옳지 않은 것은?

**보기**
- 가. 크게 부트 레코드(Boot Record), 예약된 영역(Reserved), FAT #1 영역, FAT #2 영역, 데이터 영역 등 5가지로 나누어진다.
- 나. FAT #1 영역은 클러스터들을 관리하는 테이블이 모여 있는 매우 중요한 공간으로 손상이 발생했을 때를 대비하여 백업본이 존재하며 이것이 FAT #2 영역이다.
- 다. Directory Entry에는 파일(또는 디렉토리)의 이름, 확장자, 생성·수정·접근 시간, 크기 등이 저장된다.
- 라. 긴 파일명은 Long Directory Entry를 사용하여 저장하며 해당 영역에는 파일명과 해당 파일의 속성 정보가 저장된다.

**정답:** 라

**해설**
LFN Entry는 **파일명만** 저장하고, 속성·시간·크기 등은 일반 Directory Entry에 저장된다. “LFN에 속성 정보도 저장”은 잘못.

---

## 4. (기출 16회) — FAT32(예약된 영역)

**문제**
Windows에서 사용하는 FAT 파일시스템의 경우 첫 번째 섹터의 Offset 510~511에는 Signature가 존재한다. 해당 Signature 값은?

**보기**
- 가. 0x6141
- 나. 0x55AA
- 다. 0xAA55
- 라. 0x4161

**정답:** 다

**해설**
부트 섹터 끝 시그니처는 **0xAA55**. 매체에는 리틀엔디안으로 `55 AA` 순서로 기록되므로 나(0x55AA)와 다(0xAA55)를 함께 짚었으나, 문제로 묻는 Signature 값은 다.

---

## 5. (기출 15회) — FAT32(데이터 영역)

**문제**
FAT32 파일시스템에서 부트 레코드에 아래의 값이 기록되어있는 경우 해당 파일시스템의 Data Area의 위치는 몇 번째 섹터인가?

- 섹터 크기: 512 바이트
- 클러스터 당 섹터 수: 8
- 예약영역 개수: 32
- FAT 개수: 2
- FAT 크기: 16

**보기**
- 가. 32번 섹터
- 나. 54번 섹터
- 다. 58번 섹터
- 라. 64번 섹터

**정답:** 라

**해설**
Data Area 시작 = 예약영역(32) + FAT영역(FAT크기 16 × FAT개수 2 = 32) = **64번 섹터**.

---

# 4강

---

## 1. (기출 15회·16회) — NTFS 파일시스템의 특징

**문제**
NTFS 파일시스템에서 파일의 길이가 작은 경우(일반적으로 680바이트 이하) 경우에는 그 내용을 별도의 클러스터에 저장하지 않고 다른 속성값과 함께 여기에 저장하여 효율성을 높이고 있다. 여기에 해당하는 것은 무엇인가?

**보기**
- 가. $MFT
- 나. $Volume
- 다. $LogFile
- 라. $Bitmap

**정답:** 가

**해설**
작은 파일은 MFT 엔트리 안에 Resident Data로 함께 저장한다. $Volume·$LogFile·$Bitmap은 메타데이터용이며 일반 파일 내용 저장 위치가 아님.

---

## 2. (기출 15회·18회) — 파일 삭제 후 파일시스템의 변화

**문제**
NTFS에서 파일이 삭제되었을 때 파일시스템 변화로 옳지 않은 것은?

**보기**
- 가. 파일에 대한 MFT Entry의 in-use flag를 0으로 변경
- 나. 해당 MFT Entry에 대한 $Bitmap 속성을 0으로 변경
- 다. $DATA가 Non-Resident인 경우 Cluster Run 값들을 0으로 변경
- 라. $Bitmap 파일에서 삭제된 파일에 할당된 Cluster를 Unallocated로 변경

**정답:** 다

**해설**
삭제 시 Cluster Run을 0으로 바꾸지 않고, $Bitmap에서 해당 클러스터를 비할당(Unallocated)으로 표시한다. 가·나·라는 타당한 변화, 다는 잘못된 서술.

---

# 5강

---

## 1. (기출 14회·21회) — MFT

**문제**
NTFS(New Technology File System)의 예약된 MFT(Master File Table) 엔트리(Entry) 각각에 대한 설명으로 옳지 않은 것은?

**보기**
- 가. $MFT - MFT(Master File Table)의 엔트리에 대한 정보를 담고 있다.
- 나. $LogFile - 시스템 로그에 대한 정보를 담고 있다.
- 다. $Quota - 사용자별 할당량 정보를 담고 있다.
- 라. $Bitmap - 볼륨의 클러스터 할당 정보를 담고 있다.

**정답:** 나

**해설**
$LogFile은 Windows 이벤트(시스템) 로그가 아니라 파일시스템 트랜잭션 로그이다. 가·다·라는 타당한 서술.

---

## 2. (기출 14회·15회) — MFT

**문제**
NTFS 파일시스템의 MFT(Master File Table) 속성값에 대한 설명으로 옳지 않은 것은?

**보기**
- 가. $STANDARD_INFORMATION : 파일의 생성/수정/액세스 시간, 소유자 등의 일반적인 정보
- 나. $SECURITY_DESCRIPTOR : 파일의 암호화 방식
- 다. $INDEX_ROOT : 인덱스 트리의 루트 노드
- 라. $BITMAP : $MFT와 인덱스의 할당 정보 관리

**정답:** 나

**해설**
$SECURITY_DESCRIPTOR는 접근 권한·소유자 등 보안 정보이며 암호화 방식 자체를 저장하지 않는다.

---

## 3. (기출 15회·18회) — MFT

**문제**
Windows에서 사용하는 NTFS 파일시스템의 속성(Attribute) 중 $STANDARD_INFORMATION과 $FILE_NAME이 있다. 이 두 속성에 공통으로 저장되는 정보로 옳은 것은?

**보기**
- 가. 파일명
- 나. 파일이 저장되는 클러스터 번호 목록
- 다. MAC Time
- 라. 사용자 Quota

**정답:** 다

**해설**
두 속성 모두 MAC Time(생성·수정·접근 등)을 담는다. 파일명은 $FILE_NAME만, 클러스터 목록은 $DATA, Quota는 해당 속성이 아님.

---

## 4. (기출 17회) — MFT

**문제**
다음 중 하드디스크의 특정 파티션을 NTFS 파일시스템으로 포맷하였을 때 기본으로 생성되는 파일이 아닌 것은?

**보기**
- 가. $LogFile
- 나. $Data
- 다. $Boot
- 라. $MFTMirr

**정답:** 나

**해설**
$DATA는 속성이며 포맷 시 기본 생성되는 메타파일 이름이 아니다. $LogFile·$Boot·$MFTMirr는 기본 생성.

---

## 5. (기출 20회) — MFT

**문제**
Windows에서 주로 사용하는 NTFS 시스템에서 문서 파일을 저장할 때 MFT Entry에 기록되는 Attribute 중 $FILE_NAME에 기록되지 않는 정보는?

**보기**
- 가. 파일명
- 나. 파일 플래그 (Archive, Hidden 등)
- 다. Class ID, Owner ID, Security ID 등
- 라. MAC (Modified, Accessed, Created) Time

**정답:** 다

**해설**
Class/Owner/Security ID 등은 $FILE_NAME이 아닌 다른 속성에 기록된다. 가·나·라는 $FILE_NAME에 포함.

---

# 6강

---

## 1. (기출 20회) — UFS 구조 · i-node

**문제**
유닉스 파일 시스템 구조에서 파일 소유자의 사용자 번호 및 그룹 번호, 데이터가 저장된 블록의 주소 정보를 보관하는 것은 무엇인가?

**보기**
- 가. 데이터 블록
- 나. 아이노드(i-노드) 블록
- 다. 슈퍼 블록
- 라. 부트 블록

**정답:** 나

**해설**
아이노드 블록은 메타데이터(소유자 UID/GID, 크기, 권한, 데이터 블록 주소 등)를 보관한다. 데이터 블록은 실제 내용, 슈퍼 블록은 FS 전반 정보, 부트 블록은 부팅 정보.

---

## 2. (기출) — UFS · 저널링

**문제**
다음 중 UFS 파일 시스템에 대한 설명으로 옳지 않은 것은?

**보기**
- 가. Unix 및 Unix 계열 운영체제에서 널리 사용되는 파일 시스템이다.
- 나. 시스템 내의 파일은 특정 사용자에게 소유되며, 원칙적으로 해당 사용자만이 해당 파일에 대한 조작을 수행할 수 있다.
- 다. 부트 블록, 슈퍼 블록, 실린더 그룹 등의 구조를 포함하는 파일시스템이다.
- 라. 데이터의 신뢰성을 높이기 위해 볼륨에서 수행하는 모든 작업을 트랜잭션 단위로 기록하며 비정상적인 상태일 때 복구할 수 있다.

**정답:** 라

**해설**
트랜잭션 단위 기록·비정상 시 복구는 **저널링** 기능이다. UFS는 기본적으로 저널링을 지원하지 않는다.

---

## 3. (기출) — ext 슈퍼블록 · 시그니처

**문제**
ext(예: ext2/ext3) 파일시스템 슈퍼블록의 설명 중 옳지 않은 것은?

**보기**
- 가. 슈퍼블록은 1024 바이트의 고정 크기를 가진다.
- 나. 첫 번째 두 바이트의 값 [0x53, 0xEF]이 슈퍼블록의 시그니처(매직넘버)이다.
- 다. INODE의 총 개수, 파일시스템 내의 전체 블록 개수가 기록된다.
- 라. HFS+ 파일시스템의 Volume Header와 유사한 개념의 자료구조이다.

**정답:** 나

**해설**
슈퍼블록 시그니처(매직 넘버)는 **0xEF53**이며 **오프셋 56~57번째 바이트**에 저장된다. “첫 번째/두 번째 두 바이트”라는 서술은 틀리다.

---

## 4. (기출 14회) — ext4 특징 · 저널링

**문제**
ext4 파일시스템에 대한 설명으로 옳지 않은 것은?

**보기**
- 가. 파일 데이터 할당 구조로 extent를 사용한다.
- 나. ext2부터 저널링(Journaling) 기능이 추가되었다.
- 다. 파일의 크기보다 실제 할당된 크기가 작을 수 있다.
- 라. 리눅스에서 주로 사용되는 파일 시스템 중 하나이다.

**정답:** 나

**해설**
저널링 기능은 **ext3**에 추가되었다. ext4는 ext3의 저널링을 계승·성능을 향상시킨 버전이다.

---

# 7강

---

## 1. (기출 19회) — exFAT

**문제**
FAT 12/16/32 계열의 차세대 파일 시스템으로서 FAT32의 장점을 유지하면서 파일 크기와 디렉토리 제약의 문제를 해결한 파일 시스템은?

**보기**
- 가. NTFS
- 나. EFS
- 다. exFAT
- 라. FAT64

**정답:** 다

**해설**
exFAT는 FAT32의 후속으로 대용량 파일·디렉터리 제약을 완화한다. NTFS는 FAT 직접 후속이 아니고, EFS는 NTFS의 암호화 기능이며, FAT64라는 명칭은 존재하지 않는다.

---

## 2. (기출 18회) — exFAT

**문제**
다음 중 exFAT 파일시스템에 대한 설명 중 틀린 것은?

**보기**
- 가. 10 밀리세컨드(ms)의 시간 정밀도를 지원한다.
- 나. File Directory Entry 내에 생성시간과 타임존 정보가 포함되어 있다.
- 다. File Allocation Table에서 클러스터의 할당 정보와 연결정보 모두를 표현할 수 있다.
- 라. 클러스터 번호를 표현하기 위해 32비트를 사용한다.

**정답:** 라

**해설**
exFAT에서 클러스터 번호 표현은 최대 **28비트**를 사용한다(나머지는 예약). 시간 정밀도 10ms, Directory Entry의 생성시간·타임존, FAT의 할당·연결 정보 서술은 타당.

---

## 3. (기출) — HFS+ Allocation File

**문제**
HFS+ 파일시스템의 설명 중 옳지 않은 것은?

**보기**
- 가. iOS 및 macOS에서 사용되는 파일시스템이다.
- 나. 유니코드 파일이름과 저널링(또는 자원포크)을 지원한다.
- 다. Catalog File은 B-Tree로 구성되고 파일 및 디렉토리의 구조를 표현한다.
- 라. Allocation File은 볼륨 내에 사용되고 있는 allocation block의 정보를 B-Tree로 관리한다.

**정답:** 라

**해설**
Allocation File은 **bitmap 방식**으로 관리된다. 각 비트는 allocation block 사용 여부를 나타내며 B-Tree가 아니다.

---

## 4. (기출 15회) — HFS+ 단편화 · Extents Overflow

**문제**
HFS+ 파일시스템에서 매우 단편화가 심한(20개 이상의 불연속 영역) 100MB 크기의 파일이 있다고 가정할 때, 이 파일의 데이터 전부를 읽기 위해서 참조해야 하는 두 가지 내부 자료구조는 무엇인가?

**보기**
- 가. Volume Header, Catalog File
- 나. Allocation File, Extents Overflow File
- 다. Catalog File, Attribute File
- 라. Catalog File, Extents Overflow File

**정답:** 라

**해설**
기본 extent는 Catalog File에, 초과 단편은 **Extents Overflow File**에 기록되므로 둘을 참조해야 한다.

---

## 5. (기출 20회) — APFS

**문제**
다음 글이 설명하고 있는 (파일시스템의) 명칭은 어떻게 되는지 고르시오.

> 애플의 macOS, iOS, watchOS, tvOS 등에서 범용으로 사용하기 위해 만든 파일 시스템으로 기존의 HFS+를 대체하는 새로운 파일 시스템이다. 2016년 애플 세계 개발자 회의에서 처음 공개되었고, 그 뒤 iOS 10.3 베타에서부터 본격적으로 사용하게 되었다. macOS에서는 macOS 하이 시에라부터 정식으로 도입하였다.

**보기**
- 가. exFAT
- 나. ext4
- 다. APFS
- 라. ZFS

**정답:** 다

**해설**
설명은 Apple File System(APFS). exFAT·ext4·ZFS는 각기 다른 계열.

---

# 8강

---

## 1. (기출 21회) — FAT32 파일 삭제

**문제**
FAT32에서 파일을 삭제할 때, 일어나는 일을 올바르게 기술한 것을 모두 고르시오.

- ㉠ 삭제되는 파일의 디렉터리 엔트리 첫 번째 바이트가 0xE5로 바뀐다.
- ㉡ FAT(File Allocation Table)에서 삭제하는 파일에 할당된 클러스터 정보가 모두 0으로 바뀐다.
- ㉢ 삭제되는 파일의 디렉터리 엔트리의 삭제 시간이 현재 시각으로 변경된다.
- ㉣ 삭제되는 파일의 LFN(Long File Name) 엔트리 정보가 모두 0으로 바뀐다.

**보기**
- 가. ㉠, ㉡
- 나. ㉠, ㉢
- 다. ㉡, ㉢
- 라. ㉢, ㉣

**정답:** 가

**해설**
㉠·㉡은 맞다. ㉢ 삭제 시각으로 디렉터리 엔트리를 갱신하지 않는다. ㉣ LFN을 전부 0으로 지우지 않고(LFN도 첫 바이트 0xE5 표기 등) “모두 0”은 틀리다.

---

## 2. (기출 18회) — FAT 단편화 파일 복구

**문제**
FAT 파일시스템에서 다음 그림과 같이 파일이 단편화되어 디스크에 저장되어 있다. 이 파일의 메타데이터를 복구하여 실제 파일을 복구했을 때 가장 잘 설명한 것은?

**보기**
- 가. 정상적으로 파일이 복구된다.
- 나. 파일이 손상된 상태로 복구된다.
- 다. 파일의 마지막에 해당하는 2개의 클러스터만이 복구된다.
- 라. 예상되는 클러스터보다 충분히 큰 개수의 클러스터를 복구하면 크기의 차이는 발생하지만 정상적으로 복구된다.

**정답:** 나

**해설**
단편화된 삭제 파일은 FAT 체인 정보가 사라져 연속 복구가 어려워 **손상된 상태로 복구**될 가능성이 가장 높다. (가능성 중심으로 고르는 문제)

---

## 3. (기출 15회) — NTFS Fixup Array

**문제**
NTFS를 가진 이미지 파일에서 레지던트 속성의 파일을 읽는 중 512바이트 경계의 2바이트 값이 실제 파일의 내용과 다른 것을 확인하였다. 그 이유에 대한 설명에 사용되는 용어 중 옳은 것은?

**보기**
- 가. Fixup array
- 나. FAT table
- 다. Data block Bitmap
- 라. Catalog File

**정답:** 가

**해설**
NTFS는 섹터(512B) 경계 끝 2바이트를 Fixup array로 교체·검증한다. 나·다·라는 각각 FAT/ext·HFS+ 등 다른 구조.

---

## 4. (기출 15회·18회) — $FILE_NAME

**문제**
Windows에서 주로 사용하는 NTFS 파일시스템에서 문서 파일을 저장한다고 했을 때 MFT Entry에 기록되는 Attribute 중에서 $FILE_NAME이 있다. 이 Attribute에 기록되는 정보로 옳지 않은 것은?

**보기**
- 가. 파일명
- 나. 파일 플래그 (Archive, Hidden 등)
- 다. MAC (Modified, Accessed, Created) Time
- 라. Class ID, Owner ID, Security ID 등

**정답:** 라

**해설**
Class/Owner/Security ID 등은 $FILE_NAME이 아니라 $OBJECT_ID·$SECURITY_DESCRIPTOR 등 다른 속성. 가·나·다는 $FILE_NAME에 포함.

---

## 5. (기출 15회) — NTFS 시간 정보 조작 검출

**문제**
공격자는 종종 자신이 생성한 파일을 은닉하기 위해 파일의 시간 정보를 조작한다. 다음 중 NTFS 파일시스템 시간이 조작된 파일을 검출하기 위한 방법으로 옳지 않은 것은?

참고: `$STANDARD_INFORMATION`=$SIA, `$FILE_NAME`=$FNA

**보기**
- 가. $FNA의 생성시간보다 $SIA의 생성시간이 더 이른 파일을 찾는다.
- 나. 생성시간의 초 이하 정밀도가 0인 파일을 찾는다.
- 다. $SIA의 생성시간보다 $SIA의 수정시간이 더 이른 파일을 찾는다.
- 라. 전체 파일 중 시간 정보가 동일한(동기화된) 파일을 찾는다.

**정답:** 없음

**해설**
가·나·다·라 모두 시간 조작 검출에 쓰이는 설명이다. 교재가 다를 옳지 않다고 한 것은 오류로 본다.

---

# 9강

---

## 1. (기출 15회·16회) — ext2 inode vs NTFS MFT

**문제**
다음 중 성격상 NTFS 파일시스템의 MFT와 비교하여 ext2의 inode에만 존재하는 항목은?

**보기**
- 가. last access time
- 나. inode change time
- 다. last modification time
- 라. deletion time

**정답:** 라

**해설**
deletion time은 ext2 inode에 있으나 NTFS MFT에는 별도 삭제 시각 필드가 없다. atime/mtime 등은 양쪽에 대응 가능.

---

## 2. (기출 17회) — 삭제 파일 복구 메커니즘

**문제**
파일 시스템 상의 삭제 파일 복구에 대한 설명으로 옳지 않은 것은?

**보기**
- 가. 일반적으로 파일을 삭제할 경우 파일 시스템의 효율을 위해 파일 내용을 초기화하지 않고 관련 항목만 변경시킨다.
- 나. 파일이 삭제되었으나 해당 파일 시스템의 메타데이터 영역이 덮어 쓰이지 않았다면 비교적 완벽하게 파일 내용을 복구할 수 있다.
- 다. FAT 파일 시스템의 경우, 데이터 영역의 루트 디렉토리부터 모든 하위 디렉토리를 탐색하면서 오프셋 0x00의 값이 0xE5를 갖는 Directory Entry를 찾으면, 찾은 Directory Entry에 대응되는 파일이 삭제된 파일이다.
- 라. NTFS 파일 시스템의 경우, MFT Entry 0번의 $MFT의 $BITMAP 속성에서 0x00의 값을 가지는 MFT Entry를 찾는다. 찾은 MFT Entry에 대응되는 파일이 삭제된 파일이다.

**정답:** 라

**해설**
NTFS에서 삭제 엔트리 식별은 단순히 $BITMAP의 0만으로 “삭제된 파일”이라고 단정하는 서술이 부적절. FAT의 0xE5 표기는 타당.

---

## 3. (기출 16회) — FAT32 삭제 파일 시작 클러스터

**문제**
용의자가 사용했던 USB 메모리를 분석하고 있다. USB 메모리는 FAT32 파일시스템이며 삭제된 파일이 발견되었다. (헥스 덤프·디렉터리 엔트리 구조 표 제공) 삭제된 파일(사각형 표시)의 시작 클러스터는?

**보기**
- 가. 5
- 나. 80
- 다. 1280
- 라. 알 수 없다

**정답:** 가

**해설**
삭제되어도 시작 클러스터는 엔트리에 남는다. 상위(0x14)·하위(0x1A) 조합, 덤프상 하위 `05 00` → 클러스터 **5**.

---

## 4. (기출 19회) — NTFS 파일 삭제 과정 순서

**문제**
NTFS 파일 시스템에서 'C:\test\delete.txt' 파일을 삭제하는 과정을 바르게 나열하시오.

① $LogFile과 $Extend\$UsnJrnl 파일에 로그 정보가 기록
② 상위 디렉터리 MFT Entry에서 해당 파일 인덱스 Entry 삭제
③ $MFT의 $Bitmap에서 삭제된 MFT Entry bit를 0으로 설정
④ $Bitmap 파일 $DATA에서 할당 클러스터 bit를 0으로 설정
⑤ VBR의 BPB에서 MFT 시작 위치 확인 후 이동
⑥ MFT에서 상위 디렉터리 Entry 위치 탐색
⑦ $MFT를 읽어 전체 MFT 구조 파악

**보기**
- 가. ⑤ → ⑦ → ⑥ → ② → ③ → ④ → ① *(정답으로 표시된 계열)*
- 나·다·라. 다른 순서 조합

**정답:** 가

**해설**
VBR/BPB로 MFT 위치 → $MFT 구조 파악 → 디렉터리 Entry 탐색 → 인덱스 삭제 → MFT/$Bitmap·클러스터 비트맵 갱신 → 로그 기록 순.

---

# 10강

---

## 1. (기출 16회) — 교착상태 필요조건

**문제**
프로세스는 운영체제로부터 자원을 할당받아서 작업을 수행하는데, 무한정 기다리는 교착상태(Deadlock)를 유발하는 필요조건이 아닌 것은?

**보기**
- 가. 상호 배제(Mutual exclusion)
- 나. 순환 대기(Circular wait)
- 다. 점유 대기(Hold and wait)
- 라. 선점(Preemption)

**정답:** 라

**해설**
교착 필요조건은 상호배제·점유대기·**비선점**·순환대기이다. **선점** 자체는 필요조건이 아니다.

---

## 2. (기출 16회) — 교착상태 조건 설명

**문제**
다음 중 프로세스가 교착 상태에 빠지는 조건에 대한 설명이다. 올바르지 못한 것은?

**보기**
- 가. 상호배제 조건: 하나의 프로세스가 특정 자원을 선점할 때 다른 프로세스의 해당 자원 선점을 고려하지 않아도 되는 조건
- 나. 대기조건: 적어도 하나 이상의 자원을 할당받은 채 다른 프로세스의 자원이 해제되기를 기다리는 프로세스가 존재하는 조건
- 다. 비선점 조건: 자원은 사용이 끝날 때까지 이를 갖고 있는 프로세스로부터 제거될 수 없다.
- 라. 환형 대기조건: 프로세스의 환형 사슬이 존재해서 … 자원을 가지고 있는 조건

**정답:** 가

**해설**
상호배제는 한 시점에 한 프로세스만 자원을 사용한다는 조건이며, 가의 서술은 부적절.

---

## 3. (기출 19회) — 메모리 분석

**문제**
메모리 분석에 대한 중요성으로 틀린 것은?

**보기**
- 가. 대표적인 휘발성 자료이다.
- 나. 사용자의 프라이버시를 보호할 수 있다.
- 다. 메모리에 바로 로드되는 악성코드를 확인 가능하다.
- 라. 안티 포렌식 기술과 관련 없다.

**정답:** 라

**해설**
메모리 분석은 안티 포렌식과 밀접하다. 라는 틀린 서술.

---

## 4. (기출 20회) — 가상메모리 · LRU

**문제**
컴퓨터가 메인 메모리에서 사용하기 위해 2차 기억장치로부터 데이터를 저장하고 검색하며 메모리를 관리하는 것을 무엇이라고 하며, 최근에 사용하지 않은 데이터를 교체하는 기법이 바르게 짝지어진 것을 고르시오.

**보기**
- 가. 페이징, NUR(Not Used Recently)
- 나. 페이징, FIFO(First In First Out)
- 다. 힙 스프레이, SCR(Second Chance Replacement)
- 라. 가상메모리, LRU(Least Recently Used)

**정답:** 라

---

## 5. (기출 18회) — 페이징 · TLB

**문제**
가상 메모리 구현에 일반적으로 사용되는 페이징(Paging) 기법은 가상 메모리 주소와 물리적 메모리 주소 간의 변환에 많은 CPU 시간이 낭비될 수 있다. 이런 주소 변환 시간을 줄이기 위해 사용되는 하드웨어는?

**보기**
- 가. Translation Look-aside Buffer
- 나. Page-table Base Register
- 다. Page-table Cache
- 라. Memory-space Identifiers

**정답:** 가

---

## 6. (기출 17회) — 운영체제 자원 관리 기능

**문제**
다음 중 운영체제의 자원 관리 기능이 담당하는 기능이 아닌 것은?

**보기**
- 가. 자원의 사용에 대한 제어 및 관리
- 나. 자원에 대한 동시 사용 중재
- 다. 자원의 사용에 대한 스케줄링
- 라. 자원의 독점

**정답:** 라

---

## 7. (기출 18회) — 자원의 특성

**문제**
운영체제가 컴퓨터 시스템 내에 있는 각종 자원들을 사용함에 있어 자원들에 대한 특성을 알아야 한다. 다음 중 자원의 특성에 대한 설명으로 올바르지 못한 것은?

**보기**
- 가. 공용자원: 여러 개의 프로세스가 공용으로 사용이 가능한 자원
- 나. 선점 가능 자원: … 다른 프로세스에게 자원의 사용권을 넘겨 줄 수 있는 자원
- 다. 비선점 자원: … 반환하기 전까지 다른 프로세스에게 사용권을 빼앗기지 않는 자원
- 라. 소비성 자원: 특정 프로세스가 독점하여 사용하는 자원

**정답:** 라

**해설**
소비성 자원은 사용 후 소모되는 자원이며 “독점 사용” 정의가 아니다.

---

# 11강

---

## 1. (기출 14회) — 디스크상 레지스트리 하이브

**문제**
Windows 운영체제에서 사용되는 디스크 상에 파일 형태로 존재하는 레지스트리 하이브가 **아닌** 것은?

**보기**
- 가. SOFTWARE
- 나. SAM
- 다. SECURITY
- 라. HARDWARE

**정답:** 라

**해설**
SOFTWARE·SAM·SECURITY는 일반적으로 `C:\Windows\System32\config\` 등에 파일로 존재한다. **HARDWARE**는 부팅 시 동적으로 생성되어 메모리에 유지되며 디스크상 하이브 파일이 아니다.

---

## 2. (기출 14회·21회) — 삭제된 프로그램 목록

**문제**
삭제된 프로그램의 목록을 확인하기 위해 보기의 레지스트리 경로 중 어디를 참조하여야 하는가?

**보기**
- 가. HKEY_LOCAL_MACHINE\Software\Microsoft\Windows\CurrentVersion\Uninstall
- 나. HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Uninstall
- 다. HKEY_CLASSES_ROOT\Software\Microsoft\Windows\CurrentVersion\Uninstall
- 라. HKEY_LOCAL_MACHINE\Software\Microsoft\Windows\App Management

**정답:** 가

**해설**
시스템 전역 설치/제거 정보가 쌓이는 **HKLM\...\Uninstall**을 참조한다. HKCU Uninstall은 현재 사용자 범위, HKCR는 확장자/COM, App Management는 직접적인 삭제 목록이 아니다.

---

## 3. (기출 18회) — Default 로그온 계정 Root Key

**문제**
Windows Registry에는 사용자 계정 관련 정보들이 저장되어 있다. 그중에서 Default 로그온 계정 정보가 포함되어 있는 Root Key는?

**보기**
- 가. HKEY_LOCAL_MACHINE
- 나. HKEY_CURRENT_USER
- 다. HKEY_CLASSES_ROOT
- 라. HKEY_USERS

**정답:** 가

**해설**
Default 로그온 계정 정보는 시스템 전역 설정인 **HKLM**에 있다. HKCU·HKU는 사용자 프로파일, HKCR는 클래스/확장자 정보.

---

## 4. (기출 19회) — 관리 공유(AutoShareWks)

**문제**
윈도우의 관리 목적으로 만들어진 공유 목록을 제거하기 위해 어떠한 값을 레지스트리에 추가하는 과정이다. 다음 중 **잘못된** 것은?

**보기**
- 가. 레지스트리 위치: `HKEY_LOCAL_MACHINE\SYSTEM\`
- 나. `CurrentControlSet\services\`
- 다. `LanmanServer\Parameters`
- 라. 레지스트리 값: `AutoShareWks = 1`

**정답:** 라

**해설**
경로(가·나·다)는 타당하다. 관리 공유를 **제거(비활성)** 하려면 `AutoShareWks`를 **0**으로 설정해야 하므로, 값이 **1**인 (라)가 잘못이다.

---

## 5. (기출 17회) — HWP 실행 흔적 아티팩트

**문제**
다음 중 hwp의 실행 흔적을 추적할 수 있는 아티팩트로 **가장 거리가 먼** 것은?

**보기**
- 가. 바로가기 파일
- 나. 점프목록 파일
- 다. 레지스트리 – UserAssist 키
- 라. 레지스트리 – RecentDocs 키

**정답:** 다

**해설**
바로가기·점프목록·RecentDocs는 문서 오픈/실행 경로와 가깝다. **UserAssist**는 주로 응용프로그램 실행 통계이며, 특정 **파일(hwp)** 실행 흔적과는 거리가 있다.

---

## 6. (기출 20회) — USB 연결 기록 Enum 하위 폴더

**문제**
윈도우 기반 PC에서 연결된 적이 있는 USB 정보를 확인하기 위해 `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Enum` 이하에 존재하는 폴더를 검사해야 한다. 다음 중 검사해야 할 폴더명은?

**보기**
- 가. USB
- 나. USBSTOR
- 다. STORAGE
- 라. Root

**정답:** 나

**해설**
USB 저장장치 연결·시리얼 등은 **USBSTOR**에 기록된다. 이름이 비슷한 USB·STORAGE와 혼동하지 말 것.

---

## 7. (기출 16회·20회) — USBSTOR 분석 오류 찾기

**문제**
용의자가 사용한 Windows 운영체제 컴퓨터에서 다음과 같은 정보(USBSTOR 하위 장치 목록)를 발견하였다. 다음 정보를 토대로 **잘못** 분석한 내용은 어느 것인가?

**보기**
- 가. 용의자는 3종류의 USB 외장 저장매체를 사용했다.
- 나. 용의자가 사용한 ipTIME 외장 하드디스크의 고유번호는 이다.
- 다. 용의자는 삼성 DUO PLUS 모델의 USB 메모리를 사용했다.
- 라. 용의자는 고유번호가 인 삼성 USB 메모리를 사용했다.

**정답:** 다

**해설**
레지스트리 제품 문자열은 Flash Drive FIT 등이며 **DUO PLUS**라는 모델명은 나타나지 않는다. 가·나·라는 트리에 보이는 장치 수·시리얼과 일치.

---

# 12강

---

## 1. (기출 21회) — 윈도우 기본 로그 종류

**문제**
윈도우에서 로그는 시스템 로그, 애플리케이션 로그, ( )로그가 있다.

**보기**
- 가. 알럿(Alert)
- 나. 네트워크(Network)
- 다. 크래시(Crash)
- 라. 보안(Security)

**정답:** 라

**해설**
시스템·애플리케이션과 함께 기본(메인) 로그로 구분되는 것은 **보안 로그**.

---

## 2. (기출 15회·17회) — EVTX 파일 구조

**문제**
Windows 운영체제에서 사용되는 이벤트로그(EVTX) 파일에 대한 설명으로 **옳지 않은** 것은?

**보기**
- 가. 파일 시작 시 “ElfFile”이라는 시그니처로 시작한다.
- 나. 이벤트로그 파일은 파일 헤더와 N개의 Chunk로 구성되어 있다.
- 다. 이벤트 파일의 Chunk 레코드의 시그니처는 '0x2A 2A 00 00' 이다.
- 라. 이벤트 파일의 Chunk는 항상 고정된 512바이트의 헤더를 갖고 있다.

**정답:** 라

**해설**
Chunk 헤더는 **128바이트**(512 아님). 시그니처를 “EvtxFile”로 적은 보기가 나오면 그것도 오답(실제 `ElfFile`)이라 **정답이 둘**일 수 있다고 언급.

---

## 3. (기출 21회) — 이벤트 로그와 타임라인

**문제**
윈도우 아티팩트 중 이벤트 로그 파일에 대한 설명 중 **틀린** 것은?

**보기**
- 가. 이벤트 로그에는 다양한 윈도우 이벤트 정보가 저장되지만 시간 정보는 저장하지 않기 때문에 타임라인 분석에는 사용할 수 없다.
- 나. 윈도우에서 발생하는 매일 매일의 이벤트 변화를 기록하며, 사용자가 임의로 이벤트 로그에 기록되는 이벤트의 범위를 지정할 수 있다.
- 다. 관리자는 이벤트 로그를 위한 GUI 기반의 이벤트 뷰어를 통해 이벤트 로그를 제어할 수 있다.
- 라. 이벤트 로그 데이터를 분석함으로써 시스템의 부팅, 로그인, 로그아웃, 시스템 종료, 시간 설정 변경 등에 대한 정보를 확인할 수 있다.

**정답:** 가

**해설**
이벤트 로그에는 **시간 정보가 남으며** 타임라인 분석에 활용한다.

---

## 4. (기출 18회) — 시스템 폴더 설명

**문제**
Windows 운영체제에 관한 설명으로 **옳지 않은** 것은?

**보기**
- 가. System Volume Information 폴더는 시스템 복원 도구가 사용하는 숨겨진 시스템 폴더이다.
- 나. 복원 지점에는 사용자가 만든 데이터도 저장된다.
- 다. Windows 사용자 계정 폴더의 AppData/Roaming 폴더는 AD 환경에서 동기화가 필요한 사용자 설정 등을 저장하는 경로로 사용된다.
- 라. Recent 폴더는 최근 접근한 문서 및 폴더의 링크를 저장한다.

**정답:** 나

**해설**
복원 지점은 주로 시스템 파일·설정 위주이며 **사용자 개인 데이터는 포함되지 않는다**.

---

## 5. (기출 18회) — 사용자 로그온 인증

**문제**
Windows에서 사용자 로그온 인증과 관련된 사항으로 **옳은** 것은?

**보기**
- 가. SAM에 의한 로컬 사용자 로그온 인증
- 나. MMC에 의한 도메인 사용자 로그온 인증
- 다. MCC에 의한 로컬 사용자 로그온 인증
- 라. Domain Controller에 의한 로컬 사용자 로그온 인증

**정답:** 가

**해설**
로컬 인증은 **SAM**. MMC/MCC는 인증 주체가 아니며, DC는 **도메인** 사용자 인증.

---

## 6. (기출 19회) — 시스템 복원 숨김 폴더

**문제**
Windows의 시스템 복원 도구가 해당 정보와 복원 지점을 저장하기 위해 사용하는 숨겨진 시스템 폴더는 무엇인가?

**보기**
- 가. C:\Program Files\
- 나. C:\Program Data\
- 다. C:\$RECYCLE.BIN\
- 라. C:\System Volume Information\

**정답:** 라

**해설**
복원 지점·볼륨 관련 정보는 **System Volume Information**.

---

## 7. (기출 20회) — hosts 경로

**문제**
윈도우에서 호스트 파일의 위치에 대한 경로로 옳은 것은 무엇인가?

**보기**
- 가. \Windows\system32\drivers\etc\hosts
- 나. \Windows\drivers\etc\hosts
- 다. \Windows\SysWoW64\drivers\etc\hosts
- 라. \ProgramData\drivers\etc\hosts

**정답:** 가

**해설**
`Windows\system32\drivers\etc\hosts` 경로를 정확히 기억할 것.

---

## 8. (기출 21회) — 개체 액세스 감사

**문제**
윈도우에서 제공하는 감사 항목 중 파일이나 디렉터리, 레지스트리 키, 프린터 같은 객체에 접근을 시도하거나 속성을 변경하려는 것 등을 탐지하는 것은 무엇인가?

**보기**
- 가. 로그온 이벤트 감사 (또는 로그온/로그오프 감사)
- 나. 개체 액세스 감사
- 다. 계정 관리 감사
- 라. 계정 로그온 이벤트 감사

**정답:** 나

**해설**
파일·디렉터리·레지스트리·프린터 등 **객체(오브젝트)** 접근은 **개체 액세스 감사**.

---

## 9. (기출 17회) — 서비스 사용자 계정 아닌 것

**문제**
다음의 윈도우 로컬 계정 중 **서비스 사용자 계정이 아닌** 것은?

**보기**
- 가. Administrator
- 나. Local System
- 다. Local Service
- 라. Network Service

**정답:** 가

**해설**
Local System·Local Service·Network Service는 서비스 계정 범주. **Administrator**는 관리자 로컬 계정이며 서비스 사용자 계정에 포함되지 않는다.

---

# 13강

---

## 1. (기출 19회) — 유닉스 OS 설명

**문제**
유닉스 운영체제에 대한 설명으로 **옳지 않은** 것은?

**보기**
- 가. 유닉스는 다중 프로그래밍 시스템으로 fork 시스템 호출에 의하여 쉽게 프로세스를 만들 수 있다.
- 나. 유닉스는 트리구조 디렉토리를 가진 파일 시스템을 제공하며 파일은 커널에 의하여 비구조적 바이트들의 연속으로서 지원한다.
- 다. 유닉스 프로세스 중 모든 프로세스의 조상(ancestor)인 프로세스 0은 사용자가 로그인했을 때 사용자에게 사용자 프로세스를 생성해 주는 프로세스이고, 커널 프로세스는 커널 모드에서 실행되며 프로세스 1에 해당된다. 커널 프로세스를 변경하려면 커널을 재컴파일해야 한다.
- 라. 유닉스 운영체제는 사용자, 커널, 하드웨어로 이루어지며 커널은 파일 서브 시스템과 프로세스 제어 서브 시스템의 두 요소로 구성된다.

**정답:** 다

**해설**
프로세스 0은 idle/swapper 등. 사용자 프로세스의 조상은 **프로세스 1(init)**. 보기의 “프로세스 0이 로그인 시 사용자 프로세스 생성” 서술이 잘못.

---

## 2. (기출 20회) — command.com 대응

**문제**
유닉스에서 MS-DOS의 ‘command.com’과 동일한 역할을 수행하는 것은 무엇인가?

**보기**
- 가. daemon
- 나. shell
- 다. kernel
- 라. utility

**정답:** 나

**해설**
사용자와 커널 사이 명령 해석/실행 인터페이스인 **shell**(윈도우의 cmd.exe에 대응).

---

## 3. (기출 20회) — i노드 블록

**문제**
유닉스 파일 시스템 구조에서 파일 소유자의 사용자 번호 및 그룹 번호, 데이터가 저장된 블록의 주소 정보를 보관하는 것은 무엇인가?

**보기**
- 가. 데이터 블록
- 나. i노드 블록
- 다. 슈퍼 블록
- 라. 부트 블록

**정답:** 나

**해설**
메타데이터·데이터 블록 주소(포인터)는 **i노드**. 실제 내용은 데이터 블록, 전체 정보는 슈퍼블록, 부팅은 부트 블록.

---

## 4. (기출 17회) — UNIX 디렉터리

**문제**
Linux를 포함한 UNIX 계열 시스템의 디렉터리에 관한 설명으로 **맞는** 것은?

**보기**
- 가. /bin – 시스템 관리자가 사용하는 명령어 실행 파일 저장
- 나. /var – 로그, 임시 메일 등과 같이 수시로 기록해야 하는 파일 저장
- 다. /opt – 이동식 매체의 마운트 포인트 저장
- 라. /proc – 시스템 환경설정 파일

**정답:** 나

**해설**
/bin은 필수 명령(관리자 전용은 /sbin), /opt는 추가 패키지, /proc는 프로세스·커널 상태(가상). **/var**가 가변 데이터(로그·메일 등).

---

## 5. (기출 21회) — df 명령

**문제**
UNIX 명령어 중에서 “슈퍼블록에서 카운트하고 있는 마운트된 파일 시스템, 디렉토리에서 사용 가능한 디스크 블록과 free inode수를 알려주는 기능”에 해당하는 명령어는?

**보기**
- 가. df
- 나. su
- 다. useradd
- 라. chmod

**정답:** 가

**해설**
**df**는 슈퍼블록을 참조해 용량뿐 아니라 free inode 수도 알려준다.

---

## 6. (기출 16회) — 권한 표기

**문제**
유닉스 운영체제의 파일에 대한 접근권한 표기에 설명 중 **올바르지 않은** 것은?

**보기**
- 가. `rwxr-xr-x` : 소유자는 읽기/쓰기/실행 모두, 그룹·기타는 읽기·실행만
- 나. `r-xr-xr-x` : 소유자·그룹·기타 모두 읽기·실행만
- 다. `rw-rw-rw-` : 소유자·그룹·기타 모두 읽기·쓰기
- 라. `rwx------` : 소유자·그룹·기타 사용자 모두 읽기/쓰기/실행 권한을 가지고 있음

**정답:** 라

**해설**
`rwx------`는 **소유자만** 전 권한, 그룹·기타는 권한 없음. “모두 권한” 서술이 잘못.

---

# 14강

---

## 1. (기출 15회) — Linux 공통 오브젝트(inode)

**문제**
Linux 파일시스템에 의해 사용되는 주요 공통 오브젝트 중 파일의 크기, 권한 등과 같은 다양한 속성과 페이지 캐시와 같은 정보를 관리하는 오브젝트는 무엇인가?

**보기**
- 가. 슈퍼블록
- 나. 아이노드
- 다. 디엔트리
- 라. 파일

**정답:** 나

**해설**
개별 파일 메타데이터·페이지 캐시 등은 **inode**. 슈퍼블록은 FS 전반, dentry는 이름↔inode 매핑, file 오브젝트는 열린 파일 상태.

---

## 2. (기출 16회·21회) — Linux 설명 중 부적절 항목 수

**문제**
다음은 Linux 운영체제에 관한 설명이다. **적절하지 않은 항목의 개수**는?

① md5sum은 파일에 대한 hash값을 생성하거나 변경되지 않았는지를 검증하는 도구로 사용되며 **암호화 기능을 제공한다**.
② root 권한 사용자만이 `/etc/passwd` 파일을 읽을 수 있다.
③ tripwire는 파일에 대한 변경 여부를 알 수 있도록 한다.
④ lsof는 프로세스에 의해서 열린 파일 핸들의 목록을 보여준다.
⑤ `/etc/shadow` 파일에서는 암호화된 형태로 password를 저장한다.

**보기**
- 가. 1개
- 나. 2개
- 다. 3개
- 라. 4개

**정답:** 나

**해설**
① md5sum은 해시/무결성이지 암호화 아님. ② `/etc/passwd`는 일반 사용자도 읽기 가능(제한은 shadow 쪽). ③④⑤는 적절 → 부적절 **2개**.

---

## 3. (기출 17회) — 레드햇 계열 아닌 배포판

**문제**
리눅스는 크게 레드햇 계열과 데비안 계열로 구성된다. 다음 중 **레드햇 계열의 배포판이 아닌** 것은?

**보기**
- 가. Red Hat Enterprise
- 나. CentOS
- 다. Kali Linux
- 라. Fedora

**정답:** 다

**해설**
Kali는 **데비안** 계열(보안·포렌식 용도). 가·나·라는 레드햇 계열.

---

## 4. (기출 17회) — 네트워크 상태 명령

**문제**
Linux 활성 시스템에서 **네트워크 상태 정보**를 확인하고자 할 때 사용하는 명령어로 옳은 것은?

**보기**
- 가. route
- 나. nbtstat
- 다. netstat
- 라. traceroute

**정답:** 다

**해설**
연결·포트 등 상태 확인은 **netstat**. route는 라우팅, nbtstat은 Windows NetBIOS, traceroute는 경로 추적.

---

## 5. (기출 18회) — Disk Full과 로그 관리

**문제**
로그 Volume이 작아 Disk Full이 자주 나고 Volume 확장이 어려운 상황에서 선택할 도구로 가장 적절한 것은?

**보기**
- 가. klogd
- 나. syslogd
- 다. logrotate
- 라. crond

**정답:** 다

**해설**
**logrotate**로 로그 회전·압축·삭제하여 디스크 고갈을 완화.

---

## 6. (기출 14회) — macOS fseventsd 경로

**문제**
Mac OS에서 Mount/Downloads/Trash Activity 등 파일시스템 이벤트를 확인할 수 있는 파일이 저장된 경로는?

**보기**
- 가. `/var/log/.filesystemevt/`
- 나. `/var/log/.volevt/`
- 다. `/.fseventsd/`
- 라. `/.filesysteminfo/` (또는 `/filesysteminfo/`)

**정답:** 다

**해설**
이벤트는 **`.fseventsd`** 디렉터리에 기록. 나머지 경로는 오답 트랩.

---

## 7. (기출 14회·21회) — iOS 직렬화(bplist)

**문제**
Apple사가 만들고 iOS에서 널리 사용되기 때문에 증거물 분석 시 반드시 살펴보아야 하는 직렬화 방법은 무엇인가?

**보기**
- 가. Protocol Buffer
- 나. Flat Buffer
- 다. Binary Property List (bplist)
- 라. Message Pack

**정답:** 다

**해설**
Apple 생태계의 **bplist**. 가·나·라는 Apple 표준이 아님.

---

# 15강

---

## 1. (기출 15회) — 웨어 레벨링 / FTL

**문제**
다음 설명의 ( )에 들어갈 단어를 순서대로 나열한 것은?

> NAND 플래시의 수명을 연장하기 위해 특정 블록에 치우침 없이 균등하게 쓰기 연산을 분배하는 알고리즘을 ( )라고 하고 이 알고리즘은 ( )내부에 구현된다.

**보기**
- 가. 웨어 레벨링 (Wear Leveling), FTL (Flash Translation Layer)
- 나. 스와핑 (Swapping), 커널 (Kernel)
- 다. 저널링 (Journaling), 파일시스템 (Filesystem)
- 라. 페이징 (Paging), 운영체제 (Operating System)

**정답:** 가

**해설**
균등 쓰기 분배는 **웨어 레벨링**, 구현 위치는 **FTL**.

---

## 2. (기출 17회) — SSD TRIM과 포렌식

**문제**
SSD의 TRIM 명령에 관한 설명으로 **옳지 않은** 것은?

**보기**
- 가. 특정 명령어를 통해 자동 TRIM 기능을 활성 및 비활성 할 수 있다.
- 나. 삭제된 데이터의 공간을 미리 비워두는 기능이다.
- 다. SSD의 파일쓰기 성능 저하를 막기 위해 등장하였다.
- 라. TRIM 기능은 포렌식 분석에서 파일의 복구와 관련이 없다.

**정답:** 라

**해설**
TRIM은 삭제 데이터 복구를 **매우 어렵게** 만들어 포렌식과 직접 관련. 활성 SSD에서는 사실상 복구가 힘든 경우가 많다.

---

## 3. (기출 16회) — TRIM 설명

**문제**
TRIM에 대한 설명으로 **옳지 않은** 것은?

**보기**
- 가. 운영체제에서 파일을 삭제하면 FTL에서 즉시 또는 일정 시간 후에 삭제한 파일에 속한 섹터의 물리적 페이지 대응정보를 삭제하는 것이다.
- 나. 운영체제가 TRIM을 적절한 시점에 호출하면 SSD의 성능이 좋아진다.
- 다. 사용하지 않은 페이지들을 모아서 블록단위로 지우는 가비지 컬렉션과 밀접한 관계가 있다.
- 라. HDD에서도 TRIM을 사용하면 성능이 좋아진다.

**정답:** 라

**해설**
TRIM은 **SSD(플래시)** 전용. HDD에는 적용되지 않는다.

---

## 4. (기출 14회) — HPA/DCO

**문제**
HPA/DCO에 대한 설명으로 **옳지 않은** 것은?

**보기**
- 가. Linux의 `hdparm`으로 HPA/DCO 존재 유무를 식별할 수 있으나, **제거는 불가능**하다.
- 나. HPA는 ATA/ATAPI-4, DCO는 ATA/ATAPI-6에서 소개되었다.
- 다. 최신 EnCase(7.x, 8.x)에서 HPA/DCO 존재 유무를 식별할 수 있다.
- 라. Windows 볼륨 속성으로 HPA/DCO 존재 유무를 식별하는 것은 불가능하다.

**정답:** 가

**해설**
`hdparm`으로 **식별뿐 아니라 제거도 가능**하다고 설명. “제거 불가” 서술이 잘못.

---

## 5. (기출 16회) — ARM TrustZone

**문제**
본인인증, 결제 등 민감한 개인정보를 안전하게 처리할 수 있도록 ARM 코어에서 제공하는 보안실행 모드는?

**보기**
- 가. TrustZone
- 나. Kinibi
- 다. KNOX
- 라. QSEE

**정답:** 가

**해설**
ARM 코어가 제공하는 모드는 **TrustZone**. 나·다·라는 연관 TEE/플랫폼이지만 ARM 코어 제공 모드라는 질문에 대한 정답은 TrustZone.

---

## 6. (기출 20회) — 안드로이드 파일시스템 아닌 것

**문제**
다음 중 안드로이드 파일시스템이 **아닌** 것은?

**보기**
- 가. YAFFS2
- 나. Ext4
- 다. VFAT
- 라. iOS

**정답:** 라

**해설**
iOS는 안드로이드 FS가 아님.

---

## 7. (기출 14회) — Android 소프트웨어 스택

**문제**
Android의 소프트웨어 스택에 **포함되지 않는** 것은?

**보기**
- 가. Linux 커널
- 나. 하드웨어 추상화 계층
- 다. Java API 프레임 워크
- 라. 사용자 App

**정답:** 라

**해설**
커널·HAL·프레임워크는 스택 구성 요소. **사용자 App**은 스택에 포함되지 않는다고 구분.

---

## 8. (기출 20회) — ADB

**문제**
안드로이드 ADB에 관한 설명으로 **옳지 않은** 것은?

**보기**
- 가. USB 연결 상태에서 ADB forward로 TCP 통신 가능
- 나. Android SDK에 포함
- 다. ADB로 앱 설치·삭제 가능
- 라. ADB pull로 사용자 데이터 파티션의 **모든** 파일을 획득할 수 있다

**정답:** 라

**해설**
일반 권한으로는 제한. **모든** 파일 획득에는 **루팅** 등 전제가 필요하나 보기에는 그 조건이 없어 옳지 않음.

---
